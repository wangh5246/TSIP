---
type: daily
project: waybill-ruc
date: 2026-07-15
status: active
tags: [waybill, server, readiness, formal-scale]
---

# 2026-07-15

## 本次目标

判断 WayBill 是否已经具备论文级大规模服务器实验前置条件，并冻结“跑哪些组合才有
论文价值”的正式矩阵；用户明确要求服务器只运行正式实验，不接受简化版本。

## 审阅结论

- M0–M3 方法门禁已全部 verified，但正式服务器 readiness 为 No-go；
- 当前主要阻断为 clean Git SSOT、Linux 环境锁、完整数据、正式编排/恢复、目标主机
  receipt、全量统计矩阵和隐私攻击矩阵；
- 本机 M1 仅 226 periods、M3 仅 24 instances，属于方法门禁证据，不是全量正式结果；
- 当前 WayBill V6 主要新增文件未被 Git 跟踪，服务器 clone 无法得到本机版本；
- M2 使用 macOS `/usr/bin/time -l`，不能直接在 Linux 正式运行；
- 旧 RiseFL/TSIP server runbook 不等于 WayBill V6，且多个引用脚本当前为 0 bytes；
- 本地 T-Drive 仅 24 个轨迹文件，必须先关闭完整性与授权数据 transfer 门禁。

详细证据：[[Results/Reports/服务器正式实验就绪性审计-2026-07-15]]。

## 已批准的正式路线

- S1：四真实数据集全量 fallback/公平/收益安全；
- S2：16/16 depth × fixes 全部真实 proof，每配置 1 warm-up + 10 measured，并做并发压力；
- S3：全量 canonical instances × 4 buckets × 3 attacker radii 的 certified bounds；
- S4：route inference、跨期 linkability 与 opening 泄漏；
- S5：charger/verifier 系统负载，只作补充，不替代 field trial。

唯一正式规格：[[Experiments/M4-服务器正式大规模实验]]。

## 下一检查点

先关闭 RG0–RG5/RG7/RG8；然后在目标服务器关闭 RG6。RG0–RG8 全部 pass 前不生成
formal run id，不把任何 debug/preflight 输出接入论文主表。

