---
type: daily
project: waybill-ruc
date: 2026-07-14
status: active
tags: [waybill, ruc, daily, v6, gates]
---

# 2026-07-14

## 本次目标

实现并以 artifact receipt 关闭 [[Experiments/M0-Canonical-Policy-Binding]]、
[[Experiments/M1-三层Fallback与双侧保证]]、
[[Experiments/M2-Tariff树深度与Fix规模]] 和
[[Experiments/M3-E3求解器收敛与界]]；以硬门禁而不是以“代码已写”判定完成。

## 已完成事项

- M0 已通过：canonical 正向 1/1 accepted，12/12 单因素攻击 rejected，
  fail-open=0；charger 对本地 canonical profile 做 fail-closed binding。
- M1 已通过：226 periods、3390 paired rows 的 withholding/undercharge 为 0，
  停车零费，四数据集 P95 均改善；真实 V6 proof 与 endpoint/month-close 门禁通过。
- M3 已通过：冻结模型的 24/24 实例 certified，全部
  `LB ≤ UB_rect ≤ UB_ind`、gap=0，oracle/strict checker 方向正确。
- M2 已完成 16/16 compile/constraint receipts、10,000-cell depth-14 tariff root 与
  membership 双实现复核、官方 power-22 PTAU 校验。
- M2 depth-14 的 fixes 25/50/100/200 均已完成 warm-up + 3 次实测；
  12 份 measured proof/public 全部独立验证，proof hash 均不同，
  tariff root/profile commitment/bill/public signal 与私有 Merkle path 篡改全部被拒绝。
- 对 M2 aggregate 增加了 expected-config set、R1CS/WASM/zkey/vkey/profile/input/proof 哈希链、
  PTAU BLAKE2b、fresh tariff root、12 条 membership path 双实现和 12 份 proof 终端复验，
  防止 stale/编辑过的 receipt 产生假阳性 hard gate。
- M2 的 16/16 真实 setup receipt 全部 ready、0 failed；12 个非 depth-14 临时 zkey
  在记录哈希/大小并导出 vkey 后清理。最终 `m2_hard_gate_passed=true`。

## 当前执行点

- M0–M3 状态均为 `verified`；四项方法门禁已经关闭。
- M2 最终 summary SHA-256 为
  `b5b996c9f54a4ba9dd93bed805390f11e615e412e130109ee183cb100dc78f2e`，
  final-validation run ID 为
  `573a965852fda4311f86bcd5d123a9af8782aa43ff82bb1f75a7f82bef5b2f22`。

## 证据边界

- M0 当前是 authority ID + commitment/hash pinning，没有 authority digital-signature chain。
- M1 结论依赖 authenticated、monotone、calibrated odometer，不是现场认证。
- M2 的 5/10/12 是 dimensionless normalized capacity tiers，不是 Rome 现实 RUC、
  不是 cents/m；benchmark zkey 没有 circuit-specific production contribution。
- M3 只认证冻结模型与当前 24 个实例，不外推到任意攻击模型。
- 运行中 macOS/iCloud 曾两次迁移桌面仓库路径；仅一次 n100 warm-up 因
  ENOENT 失败，失败回执被保留，恢复路径后 n100 与后续 proof 均重跑通过。

## 下一检查点

执行全套 targeted tests、project KB sync/lint/link check；下一研究阶段进入可信输入与
真实账单证据、E5 隐私、相机部署基线和论文重写。
