---
type: daily
project: tsip-heatmap
date: 2026-07-13
updated: 2026-07-13
status: active
tags: [tsip-heatmap, daily, sync]
---

# 2026-07-13

## 本次目标

- 判断 V4 N=1000 大规模实验当前真实状态。
- 将 2026-06-18 以来所有未同步 Heatmap 对话去重写入 Obsidian。
- 建立 `Research/tsip-heatmap/` 规范项目层，同时保留旧 `TSIP_heatmap/` 内容。

## 状态核验

- `launch_status.json`：13/15 completed，仍残留 `state=running`。
- Rome/303 与 Synthetic/303：各 3/16，最后更新时间 2026-07-12 12:18 +0800。
- 宿主机无 launcher、protocol worker 或 caffeinate 进程。
- health endpoint 可达但 proof total 不增长；client containers 只有 `sleep infinity`。
- 结论：两个单元中断，当前完整矩阵未完成。

## 同步产物

- [[Sources/Notes/Codex对话同步审计-2026-07-13]]
- [[Knowledge/V4方法与证据边界]]
- [[Experiments/V4-Docker-N1000完整实验]]
- [[Results/Reports/V4实验结果-2026-07]]
- [[Writing/论文修订与投稿决策]]
- [[Sources/Data/五数据集N1000输入与用途]]

## 工具问题

官方 KB scaffold 因本机缺少 `/Users/wanghao/.agents/templates/` 模板而中止。目录已由脚本创建，其余 scaffold、binding 和笔记按相同 schema 手工补齐，随后使用独立检查器验证。

## 下一检查点

续跑前先清理两个空闲 compose 项目；15/15 后更新 Hub、实验、结果报告和 Daily，不覆盖本次中断事实。

## TDSC 修订检查点

- 用户确认 IEEE TDSC 为唯一目标并批准方案 A。
- 已按 `research-paper-writing` 和 evidence-first 原则写入修订设计：`docs/superpowers/specs/2026-07-13-tsip-heatmap-tdsc-revision-design.md`。
- 设计已完成 placeholder、矛盾、范围和完成条件自检，并单独提交为 `c6243a02`；未包含工作区其他 staged changes。
- 当前稿快速审计：Abstract 约 247 词；PDF 为 7 月 7 日旧 17 页；正文仍是两数据集加 N=50；`main.tex` 有 staged-delete/untracked-replacement 风险。
- Artifact 复核：archive compatibility relation 为 2,728/14/23，manifest-bound paper R1CS 为 3,547/16/25；正文 3,056/14/23 属于待修 P0。
- 下一门禁：用户复核书面规格，通过后生成详细实施计划，再开始改稿。

## 19:37 规格与续跑检查点

- 用户已明确回复“规格通过”；设计规格进入实施计划阶段，尚未开始改稿或新增实验进程。
- 已生成两份自包含计划：`docs/superpowers/plans/2026-07-13-tsip-heatmap-tdsc-pre-scale-revision.md` 与 `docs/superpowers/plans/2026-07-13-tsip-heatmap-tdsc-final-scale-handoff.md`。
- 计划自审已补上严格 scale aggregate receipt 门禁：launcher 的 15/15 状态本身不能把论文证据升级为 verified，必须逐项验证 15 个 `status.json`。
- 外部实验状态在计划编写期间发生变化：19:11 +0800 启动了新的 launcher；19:37 时 PID 9233、Rome/303 PID 9354、Synthetic/303 PID 9353 均存在。
- `launch_status.json` 仍为 13/15 running；两个未完成单元均已推进到 2/16，容器 healthy 且 client/shuffler 有持续 CPU 使用。结论更新为“续跑中”，不是此前的空闲容器状态。
- 当前操作纪律：不执行 compose down、不启动第二个 launcher，只读监控现有进程；只有宿主进程消失且 receipt 未通过时才进入恢复分支。

## 19:41 实施计划交付检查点

- 两份计划已通过 placeholder 扫描、Markdown fence 平衡、6 个 Python 代码块编译、45 个 Bash 代码块语法检查及 whitespace 检查。
- 计划已独立提交为 `ed4d2f86`（`docs: plan heatmap TDSC revision execution`），提交只包含两份 plan 文件。
- 原有 `TSIP/main.tex` staged deletion 加同路径 untracked replacement 状态保持不变，未混入计划提交。
- pre-scale 计划覆盖稿件保全、manifest-bound evidence builder、合同检查、全文修订、五数据集固定公平结果、数据/引文/许可证门禁、构建与逐页检查。
- final-scale 计划优先保护当前 active resume，仅在无 launcher 且 receipt 未通过时恢复；15/15 结论还必须经过 strict aggregate receipt。
- 下一门禁：用户在 Subagent-Driven 与 Inline Execution 中选择执行方式；未选择前不执行计划中的论文改动。

## 19:49 Pre-scale Task 1 手稿保全回执

- 历史 starting HEAD（不是 canonical working replacement）：`ed4d2f86:TSIP/main.tex` 的 Git blob 为 `5770e2f7ba7420522bd2387754e20a9d85523cac`，SHA-256 为 `35e9e955d98908eaf081f7528c266c6371911b88f10ba6e49e5b0792492266ac`。
- `TSIP/main.tex` canonical working SHA-256：`695cb9787adc86b05b40706aceb4eb6dec4c008fb04eaa92e3822fe5c70596b4`；写入 Git object storage 的 blob 为 `d70028223a1fab8b8f9fd0e44f051578b0cab87a`。`git hash-object` 复算一致，`git cat-file blob` 可恢复相同的 103,807 bytes 与 SHA-256。
- `TSIP_heatmap_version/paper/main.tex` isolated-paper 已使用 `git hash-object -w` 写入 Git object storage，返回并复算为 blob `cad8f481e2a05af962eaf3a86573dad84a4854fc`；`git cat-file blob` 可恢复相同的 103,687 bytes 与 SHA-256 `78703d7e978d02c200e5d2d99ef3696c5c2381bde10c4c31dd63b3d31f37e6be`。
- 保全提交：`2636cec87458d67efd8a3960a7a5d99d74263d03`（`chore: preserve active heatmap manuscript`），父提交为 `ed4d2f869142c4cdedd4c1067bc75901833e88a8`；提交只包含 `TSIP/main.tex`，其 committed blob 为 `d70028223a1fab8b8f9fd0e44f051578b0cab87a`。原有 5,653 个 unrelated staged paths 保持 staged 且未提交，pre/post staged diff SHA-256 均为 `9b93dae3e047ce7237152f60d2ce4b918f40316c0a8e0f034ac009984fb0d985`。从仓库根目录运行的可复现命令为 `git diff --cached --binary -- . ':(exclude)TSIP/main.tex' | shasum -a 256`。
- 对比结论：完整 isolated-paper delta 共 118 行、5 个 hunk，均位于 Evaluation，内容是五数据集 `N=1000` 的数据集/参数说明、RQ3 metadata leakage、RQ4 in-envelope pollution，以及 RQ5 utility 与图注的 generated-paper context；未复制或覆盖任一来源。
- canonical-source 决策：`TSIP/main.tex` 是唯一手工编辑的 canonical manuscript；`TSIP_heatmap_version/paper/main.tex` 仅作为 candidate material，只有在后续 build verification 通过后才可成为 mirror。本步骤不完成或勾选任何 writing task。
- 两层审查结论：补齐 starting HEAD identity、isolated-paper durable blob、可复算 staged-diff 命令与 lint 明细后，spec compliance 与 engineering quality 均通过；Task 1 正式完成。
- KB sync/lint 回执（2026-07-13 20:06:14 +0800）：broken links 0；missing/dangling registry entries 0/0；missing index entries 0；knowledge without sources 0；experiments without results / with only archived results / results without experiments 0/0/0；daily promotion candidates 0；active notes referencing archived notes 0（canvas issues 亦为 0）。
- Pre-scale Task 2 已完成：新增 `script/build_v4_paper_evidence.py`、`tests/test_build_v4_paper_evidence.py`、`TSIP_heatmap_version/runtime/experiments-heatmap/docker_v4_smoke/analysis_v4/paper_evidence.json`、`TSIP/tables/v4_paper_evidence_macros.tex` 与 `TSIP/tables/tab_v4_fixed_utility.tex`；TDD red 为动态导入缺少脚本的 `FileNotFoundError`，green 为 `python -m pytest -q tests/test_build_v4_paper_evidence.py` 5 passed，配置基线 `python -m pytest -q` 45 passed；manifest-bound circuit 为 3,547/16/25，static/protocol/utility 均 pass，scale 因 launcher 仍为 13/15 running 且严格 `aggregate_summary.json` 缺失而保持 partial，此为唯一未解决门禁。
- Pre-scale Task 2 spec-review 修复：仅增强 `tests/test_build_v4_paper_evidence.py`，使用五组互异 utility 指标逐行锁定 T-Drive、GeoLife、Porto、Rome、Synthetic 的 canonical 顺序、SHTPC/Nebula mean 与 std、delta、FRR、MRR、恰好五行及全部数值三位小数格式；production code 未改，`python -m pytest -q tests/test_build_v4_paper_evidence.py` 为 5 passed，配置基线 `python -m pytest -q` 为 45 passed。
- Pre-scale Task 2 quality-review 修复：`script/build_v4_paper_evidence.py` 与 `tests/test_build_v4_paper_evidence.py` 已对 strict 15-unit scale schema、九项 manifest artifact 集合与 repository-relative/no-escape 路径、22 项 static 细节、五数据集 protocol returncode、utility finite/range/delta、launcher exact-int 及 source receipt SHA-256 binding 全部 fail closed；TDD red 为 39 failed/10 passed，green 为 focused 49 passed，配置基线 45 passed；重新生成的 `paper_evidence.json` 绑定 manifest/static/protocol/utility/launch 五份来源，当前 circuit 3,547/16/25、三门禁 pass、scale 仍为 13/15 running/partial、strict summary hash 为 null，LaTeX 两文件字节未变。
- Pre-scale Task 2 second quality re-review 修复：strict scale summary 现须绑定本次 manifest/launch SHA-256 且 15 个 unit receipt hash 互异；新增并绑定 fixed utility `status.json`，强制五数据集顺序、seeds 101/202/303、N=1000、10 rounds 与双 utility gate；static 强制精确 22 项身份，protocol 强制 exact-int `dataset_count=5`，所有 source receipt 输入强制 repository-relative/no-traversal/no-symlink-escape，utility 输出仅保留已验证字段，JSON 非有限值序列化 fail closed。TDD red 分别为 8 failed/50 passed、6 failed/58 deselected、22 failed/64 deselected；最终 focused 86 passed、配置基线 45 passed。真实证据重新生成后 circuit 仍为 3,547/16/25，static/protocol/utility 全 pass，utility caption 已由 companion status 绑定，source receipts 增至六份；launcher 仍为 13/15 running，strict summary 不存在，scale 正确保留 partial 且 summary hash 为 null。
- Task 2 最终审查：spec compliance 与 engineering quality 均通过；最终质量复审确认此前 artifact、路径、数值、launcher、source hash、run binding、utility caption、static/protocol identity 问题全部关闭，无 Critical/Important/Minor 遗留。

## 21:29 Pre-scale Task 3 手稿合同检查器检查点

- Task 3 新增 `script/check_heatmap_manuscript.py` 与 `tests/test_check_heatmap_manuscript.py`；TDD red 为 focused pytest 动态导入时报预期 `FileNotFoundError`，green 为 27 passed，evidence-builder/checker combined 为 113 passed，配置基线为 45 passed，owned `git diff --cached --check` 为 0；canonical `TSIP/main.tex` 检查按预期 exit 1，报告 Abstract 248 词、3,056/14/23 与 `eq:r-tsip` 陈旧事实、Porto/Rome/Synthetic 缺失及三项 V4 artifact macro 缺失。证据状态保持 scale partial 13/15；Task 3 无实现阻塞，未解决项继续由 Tasks 4-6 手稿修订与 live scale 后续门禁处理。

## 21:47 Pre-scale Task 3 规格复审修复检查点

- Task 3 follow-up 仅修改 `script/check_heatmap_manuscript.py` 与 `tests/test_check_heatmap_manuscript.py`：completion claim 现覆盖 `15/15`/`all 15` 与可选 `dataset-seed`，所有合同门禁先剥离 LaTeX comments，dataset 使用 lexical boundaries，artifact macro 定义不再算作正文使用，3,056/14/23 与 `eq:r-tsip` stale targets 使用精确边界。回归 TDD red 为 25 failed/48 passed，green 为 focused 73 passed；Task 2/3 combined 为 159 passed，配置基线为 45 passed；canonical 检查仍按预期 exit 1 且保持相同 11 issues/Abstract 248 词。证据状态仍为 scale partial 13/15；Task 3 无 blocker，Tasks 4-6 与 live scale 门禁保持未解决。

## 22:47 Pre-scale Task 3 最终复核与执行模式检查点

- 子代理平台额度已耗尽，无法取得新的独立 agent review；此限制没有被伪装为审查通过。用户要求继续后，后续改为当前会话 inline execution，并保留 TDD、规格复核、质量复核、精确路径提交及每任务 Obsidian lint 门禁。
- Task 3 本地独立规格复核通过：计划要求的 Abstract 100--200 词、3,056/14/23 与 `eq:r-tsip` 陈旧事实、五数据集、三项生成宏真实使用、partial scale 禁止提前宣称 15/15 均有实现和正反边界测试。
- Task 3 本地质量复核未发现遗留问题：`python -m pytest -q tests/test_check_heatmap_manuscript.py` 为 73 passed；Task 2/3 combined 为 159 passed；`python -m pytest -q` 配置基线为 45 passed；`py_compile` 与 owned diff whitespace check 均 exit 0。
- 当前 canonical 检查按设计 exit 1，稳定报告 Abstract 248 词及 11 个 Tasks 4--6 待修项，因此 checker 未误放行未修订稿。Task 3 正式完成，提交链为 `1cc26e9d`、`f45972dc`。
- live scale 只读复核：launcher PID 9233、Rome/303 worker PID 9354、Synthetic/303 worker PID 9353 仍存在；两个单元均为 `running`、14/16 rounds、`failure=null`。总体完成 receipt 仍为 13/15，禁止提前升级为 verified。

## 22:52 Pre-scale Task 4 电路事实一致性检查点

- `TSIP/main.tex` 已在 `\begin{document}` 前唯一加载 `tables/v4_paper_evidence_macros`；Abstract、Method、Security、RQ6 与 Conclusion 的当前 relation 均改由 `\VFourConstraints`、`\VFourPublicInputs`、`\VFourPrivateInputs` 提供 3,547/16/25，内部 label 已从 `eq:r-tsip` 改为 `eq:r-shtpc`。
- RQ6 已拆分为三层且禁止跨层复用：2,728/14/23 仅为 regenerated archive compatibility；`3\,056` 与 snarkjs K6/K30 延迟、128 B proof object 仅为 2026-05-04 historical performance receipt；3,547/16/25 才是当前 manifest-bound paper relation。
- 当前 manifest 不包含 serializer size receipt，仓库也未找到绑定当前 paper circuit 的 5,694 B frame receipt，因此 Method/Abstract/RQ6 已删除当前 frame-size 主张；128 B 只在 historical 段出现。历史 Rapidsnark、browser、Aggregator 和 native-verifier microbench 亦不再冒充当前 relation 性能。
- 规格搜索通过：旧格式 3,056、`14 public inputs`、`23 private inputs`、`eq:r-tsip` 均为 0；宏覆盖 Method/Security/RQ6/Conclusion；生成宏 input 恰好一次且位于 document 前。checker 从 11 issues 收敛为 4 issues，仅余 Abstract 239 词及 Porto/Rome/Synthetic，属于 Tasks 5--6；Task 2/3 combined 159 passed，配置基线 45 passed，`git diff --check` exit 0。
- 22:52 live scale receipt 仍为 13/15；Rome/303 与 Synthetic/303 各 14/16、`failure=null`，launcher 与 workers 均存在。Task 4 不写入 15/15、当前 latency 或 throughput 主张。
- Task 4 精确路径提交为 `e3f83329`（`fix: align SHTPC paper with manifest-bound circuit`），commit tree 只包含 `TSIP/main.tex`，69 行 diff（44 insertions、25 deletions）；Obsidian 项目文件保持独立同步，不混入该代码提交。

## 23:03 Pre-scale Task 5 Abstract 与 claim-evidence 检查点

- Abstract 已按五消息结构重写：problem significance、missing same-relation binding、SHTPC mechanism、verified evidence、scoped limitation；`check_heatmap_manuscript.py` 计数为 155 词，未写入当前 13/15 scale、latency 或 throughput。
- Abstract 的已验证证据仅来自 manifest-bound 3,547/16/25、22 static checks 与 fixed `epsilon=5`/`tau=2` 五数据集 N=1000 三种子 utility；表述明确说明五个 Jaccard delta 均为正但其中两项很小，并把 FRR/MRR 限定在该固定比较内。
- 新增 `TSIP/CLAIM_EVIDENCE_MAP.md`：五条 Abstract outline、27 个主要 claim、Introduction/Method/Evaluation/Discussion/Conclusion 共 86 个 prose paragraph/list contribution reverse-outline 条目，以及 24 个五维 adversarial reviewer questions。claim status 仅使用 `supported`、`partial`、`analytical`、`assumption`，保留的 `unsupported` 为 0。
- 对抗性复核已移除或降级四类易误读主张：Introduction 不再笼统承诺 prototype costs；Evaluation 导航不再承诺当前 report/mobile cost；旧硬件计时设置不再代表 current manifest；RQ4 cost figure 明确标成 pre-manifest historical panel。
- 自动门禁：outline 5、claims 27、reverse outline 86、self-review 24、unsupported 0；Task 2/3 combined 159 passed，配置基线 45 passed，owned `git diff --check` exit 0。manuscript checker 只剩 Porto/Rome/Synthetic 三项，均由 Task 6 处理。
- 23:03 live scale 只读状态仍为 13/15；Rome/303 与 Synthetic/303 均 14/16、`failure=null`，launcher 与 workers 仍存在。Task 5 不改变实验进程或 scale evidence status。
- Task 5 精确路径提交为 `2c02923a`（`docs: sharpen SHTPC claims for TDSC`）；commit tree 仅包含 `TSIP/main.tex` 与 `TSIP/CLAIM_EVIDENCE_MAP.md`，共 256 insertions、40 deletions。Obsidian 同步仍与代码提交隔离。

## 23:22 Pre-scale Task 6 五数据集固定证据检查点

- `TSIP/main.tex` 已把 T-Drive、GeoLife、Porto、Rome 四个 real trajectory-derived inputs 与 clustered random-walk Synthetic 接入 Experimental Setup；固定 utility 明确为每数据集 `N=1000`、10 windows、无 real-input bootstrap。
- Experimental Setup 现按三个独立段落区分 original-input utility、safe-trajectory Docker protocol smoke 与 full Groth16 scale；`TSIP_SAFE_PROTOCOL_TRAJECTORY=1` 只隔离 timestamp gap/out-of-envelope jump，不再可能被误读为 utility 来源。
- RQ5 已由生成的五数据集表和热图替换旧 GeoLife headline：固定 `epsilon=5.0`、post-admission release `tau=2.0`、seeds 101/202/303；五个 delta 全部报告为 +0.110、+0.512、+0.007、+0.108、+0.007，且 Porto/Synthetic 明确为 small gains；同一 modeled comparison 的 FRR=0、MRR=1。
- 新增可复现图生成器与 10 项 fail-closed 测试；canonical PDF 连续两次生成 SHA-256 均为 `e0efc93e91a2f7dd128ce0552eb930470f724ef9c92052ca9294ee6293ec5915`，PDF 重新栅格化视觉正常，SHTPC/Nebula、五数据集和十个 cell 数值均可提取。
- 自动门禁：manuscript checker Abstract 155 words/0 issues；Task 2/3/6 focused 169 passed；unaffected settlement 28 passed；Black check 与 Flake8（88 chars）通过；claim map 为 27 claims、89 reverse-outline entries、24 reviewer checks、unsupported 0。
- 仓库默认 pytest 当前为 11 failed/34 passed，失败全部来自并发的非 Heatmap charger policy 改动，统一报 `unknown policy jurisdiction: ruc-demo`；相关文件 `services/charger/app.py`、`common/policy_profile.py` 与 `tests/test_charger_service.py` 已在本任务前/外部处于 dirty/untracked，本任务未修改或回退。
- 23:22 live scale 仍为 13/15 aggregate；Rome/303 与 Synthetic/303 均为 `running`、15/16 rounds、`failure=null`，launcher PID 9233 与两个 workers 仍存在；未启动第二份 launcher。
- Task 6 精确路径提交为 `3f1d5d4b`（`docs: integrate fixed five-dataset V4 evidence`）；commit tree 仅包含 `TSIP/main.tex`、`TSIP/CLAIM_EVIDENCE_MAP.md`、canonical PDF、图生成器及其测试，共 338 insertions、59 deletions。实验分析目录下的重复 PDF/PNG 保持 untracked，未混入提交。

## 23:24 V4 N=1000 大规模执行完成检查点

- 现有 launcher 已自行结束，宿主机不再存在 `run_v4_docker_n1000_scale.py`、`run_v4_docker_protocol_smoke.py` 或 `caffeinate` 进程；未执行停止、重启或第二份 launcher。
- `docker_n1000_v4/launch_status.json` 已收敛为 `state=pass`、`completed_units=15`、`failed_units=0`、`total_units=15`、`failure=null`，`finished_at=2026-07-13T15:24:37Z`（北京时间 23:24:37）。
- Rome/303 与 Synthetic/303 均由本次续跑返回 `state=pass`、`returncode=0`；其余 13 个已有通过单元由启动器记录为 `skipped_pass`，没有重跑。
- 当前只能把“执行矩阵 15/15 完成”升级为直接 receipt 事实；论文中的 verified scale 统计仍须等待 strict aggregate builder 逐项验证 15 个 `status.json` 并生成绑定 hash 的 aggregate JSON/CSV，不能仅凭 launcher receipt 提前写入最终性能结论。
- Pre-scale Task 7 已开始：正在核验五个数据集的官方来源、许可、引用和再分发边界，并准备 `DATA_NOTICE.md`、artifact 索引、citation audit 与作者确认门禁。

## 23:43 Pre-scale Task 7 合规与可复现性检查点

- 新增根级 `DATA_NOTICE.md`，按统一字段记录 T-Drive、GeoLife、Porto、Rome 与 Synthetic 的类型、官方来源、必引文献、许可/条款、再分发、本地预处理和仓库内容；五个数据集各 8 个字段均完整。
- 真实数据统一采用“不发布 raw 或 row-level prepared trajectories”策略：T-Drive/GeoLife 受 Microsoft Research License Agreement 限制，Rome 按 CRAWDAD 指引不得重分发，Porto 虽为 CC BY 4.0 仍按项目统一策略只发布脚本、manifest 和聚合证据。
- `.gitignore` 已覆盖 Heatmap 四真实数据派生 JSONL 与 period 目录；`git ls-files` 确认 raw/prepared 路径跟踪数为 0，`git check-ignore` 对代表性路径命中预期规则。
- 新增 `TSIP_heatmap_version/ARTIFACT_README.md`，列出 static gate、manifest、protocol smoke、fixed utility、publication figure、scale launcher、15 个 unit receipts、环境要求、精确复现和无 `rg` 监控命令；明确 15/15 launcher receipt 不是 strict aggregate 性能结论。
- 新增 `TSIP/CITATION_AUDIT.md` 并修正/补充数据集与高风险 DP/VDAF/ZK 文献。GeoLife 采用官方指南要求的三篇引用，T-Drive 采用两篇；Prochlo 作者、GeoLife 作者与 Poplar DOI 已纠正。
- 新增作者确认清单与最小 `TDSC_SELF_REVIEW.md`。Git 历史仅显示 wangh5246 的两个邮箱，不能推导代码许可；仓库仍无 `LICENSE`，两处均保留精确门禁 `BLOCKED: explicit author license selection required`。
- `TSIP/main.tex` 已同步数据提供方条款、行级数据不发布、Synthetic 许可边界、开发 Groth16 setup 非生产 ceremony、模拟审稿仅为内部质量控制，以及二次数据使用 ethics confirmation 待作者确认。
- 自动门禁：manuscript checker 为 Abstract 155 words、0 issues；Task 2/3 focused tests 159 passed；合规路径 `git diff --check` 为 0。下一步先提交 Task 7 的独立合规单元，再完整编译、Biber、逐页视觉检查并机械同步 paper mirror。
- Task 7 精确路径提交为 `dec09503`（`docs: add heatmap provenance and submission controls`），commit tree 仅包含 `.gitignore`、`DATA_NOTICE.md`、Heatmap artifact guide、作者确认清单、citation audit、最小 self-review 与 `TSIP/main.tex`；`TSIP/reference.bib` 留待 Task 8 完整 Biber/PDF 验证后提交。

## 23:59 Pre-scale Task 8 构建与逐页检查点

- 聚焦验证再次通过：Task 2/3 tests 159 passed；manuscript checker 为 Abstract 155 words、0 issues；canonical `main.tex` 与 `reference.bib` whitespace check 为 0。
- 首次 clean compile 暴露本机 BasicTeX 缺 `pcrr7t.tfm`；系统 TeX Live 不可写，随后用 `tlmgr --usermode install courier` 安装官方 Courier font package 到用户 texmf。第二次编译继续捕获新增 `DATA_NOTICE.md` 的未转义下划线，修为 `DATA\_NOTICE.md` 后通过。
- 最终构建链 `pdflatex -> biber -> pdflatex -> pdflatex` 全部 exit 0；Biber 2.21 处理 90 citekeys，`main.log`/`main.blg` 无 undefined citation/reference、fatal、rerun、Biber WARN/ERROR。
- 最终日志：overfull 0、LaTeX warning 0、package warning 0、underfull 42。42 项均经 18/18 页视觉检查，没有对应裁切、重叠、不可读表格、空白 float 区或缺失图像。
- 批量预览曾把偶数两位页码的细前导 `1` 弱化；原分辨率 footer crop 与 PDF text layer 确认 12/14/16/18 均完整。这不是稿件缺字。
- 第 4 页无编号 coverage matrix 是正文 `Violation Families and Coverage` 明确引出的 inline matrix；第 18 页大面积留白是 90 条参考文献自然结束，不是额外空白页。
- Porto/Rome bibliography notes 已改为官方 DOI，内部 creator-list 说明只保留在 citation audit，避免审计句进入正式 bibliography。
- 新 PDF 为 18 pages、letter、500200 bytes，SHA-256 `376273f598eb5c2050caae759b5a5d9e184d465fa1066edadf3c5f4e8e9f6888`。
- canonical/mirror 已机械同步并 `cmp` 通过：两份 `main.tex` SHA-256 均为 `5fd63ecacee9874dbd122d9ab442331ed27e75743b8eec4702a7e60b626bfea4`；两份 `reference.bib` 均为 `c1c4d078c2e2acf21a204312d2f62bb2a0e71b96e17b50926d35d11ededfb6cf`。
