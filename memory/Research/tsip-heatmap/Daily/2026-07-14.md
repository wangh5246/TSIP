---
type: daily
project: tsip-heatmap
date: 2026-07-14
updated: 2026-07-14
status: active
tags: [tsip-heatmap, daily, sync]
---

# 2026-07-14

## 当前目标

- 完成 pre-scale Task 8 verified-paper 精确提交。
- 完成 Task 9 adversarial self-review 与 Obsidian 闭环。
- 为 15/15 N=1000 Docker receipts 构建 strict aggregate JSON/CSV/LaTeX 并绑定 paper evidence。
- 完成 final-scale 论文传播并执行最终 clean build、全页视觉 QA 与 mirror 同步。

## 跨日状态

- 2026-07-13 的 Task 7 合规提交为 `dec09503`；仓库 license 仍为 `BLOCKED: explicit author license selection required`。
- Task 8 clean build 已完成：Biber 90 citekeys，PDF 18 页，undefined/overfull/LaTeX/package warnings 均为 0，42 个 underfull 经 18/18 页视觉检查无可见缺陷。
- canonical 与 paper mirror 已字节同步；Task 8 已以精确路径提交为 `d54ac1a5`（`docs: verify pre-scale SHTPC TDSC manuscript`）。
- N=1000 Docker execution matrix 与 strict aggregate 均为 verified；canonical paper-evidence、RQ6、Abstract、Conclusion 和 CE23 已完成写回，当前等待最终构建门。

## 工具记录

- 跨午夜 sync 再次暴露本机缺少 `/Users/wanghao/.agents/templates/notes/daily.md`；本 Daily note 按现有项目 schema 手工建立后继续运行官方 sync/lint。

## Task 8 收口

- 提交只包含 canonical `main.tex`、`reference.bib`、18 页 `main.pdf` 与两个 paper mirror 文件，没有带入工作区其他暂存内容。
- 该提交是 pre-scale verified manuscript checkpoint；15/15 execution receipts 的严格聚合、论文统计写回和最终重编译仍属于后续 final-scale gate。

## Task 9 五维对抗自审

- `TSIP/TDSC_SELF_REVIEW.md` 已扩展为 contribution、writing clarity、experimental strength、evaluation completeness 和 method soundness 的完整审查，并写入构建收据、作者确认门和 exact deferred scale work。
- 审查删除了 MDS/EETS 可证明 HBC/非合谋现实可行的过强类比，改为 distinct administrative domains、access documentation 与 cross-role audit 的显式假设和失效条件。
- claim map 现为 17 supported、7 analytical、2 assumption、1 partial、0 unsupported；CE01 citation 与 CE26 artifact 已关闭，CE23 strict scale 为唯一 partial。
- 固定配置 `per_seed_summary.csv` 的 15 个 dataset-seed 对全部满足 SHTPC Jaccard > Nebula，最小差值 0.002391152；继续把 Porto/Synthetic 标为 small gains。
- 修订后 clean build 仍为 18 页；Abstract 155 词、checker 0 issue、undefined/overfull/LaTeX/package/Biber warnings 均为 0、underfull 42，Threat Model 对应第 5--7 页重新视觉检查通过。
- 当前哈希：`main.tex` `77e57e...e12`，`main.pdf` `b28bc8...fcb`；canonical/paper mirror 字节一致，169 个聚焦测试全部通过。
- 仓库 license 仍为 `BLOCKED: explicit author license selection required`；不创建 `LICENSE`。
- Task 9 已精确提交为 `a0db10db`（`docs: record SHTPC TDSC pre-scale review`）；该提交同时将 Heatmap Obsidian 项目目录纳入版本控制。

## Final-scale Task 1 只读分类

- `docker_n1000_v4/launch_status.json` 为 `state=pass`、`completed_units=15`、`total_units=15`、`failed_units=0`，完成时间为 2026-07-13 23:24:37 +0800。
- Rome/303 与 Synthetic/303 均为 `pass`、16 rounds、`failure=null`；宿主机无 launcher/worker/caffeinate 进程，无活动 v4n1000 容器。
- 分类结果为 `COMPLETE`；不执行 compose down、恢复或重跑。`aggregate_summary.json` 与 `aggregate_by_dataset.csv` 尚不存在，下一步为 strict summarizer TDD。

## Final-scale Task 2 strict aggregate

- 按真实 receipt schema 建立 33 项 fail-closed tests，并实现 `script/summarize_v4_n1000_scale.py`；合并 paper evidence/manuscript/figure tests 后为 202 passed。
- 第一次实际聚合暴露 Rome/101 与 Synthetic/101 的 21.4 h wall time 包含宿主休眠；两个 `round_id` 最大间隔约 62,962 s，超过 7,200 s round-timeout，不能直接进入论文吞吐统计。
- 修正后规则保留全部 15 个功能 receipts，仅从 timing statistics 排除上述 2 单元，并强制每数据集至少两个 timing units。
- 严格总账：15 units、240 rounds、225,000 proofs generated/verified、0 failed、150,000/150,000 A/R、150/150 reconstruction、150/150 DP。
- 13 个连续计时单元均值为 `1.000 +/- 0.069 proofs/s`，范围 0.945--1.216，单元均值 wall time 4.183 h。
- JSON/CSV/LaTeX SHA-256 分别为 `2ad8e3...16a95`、`11050a...bbe4`、`d16120...af43`；连续两次生成字节一致，临时 paper-evidence 门返回 `scale.status=verified`。
- Task 2 已提交为 `248f858c`（`feat: summarize verified V4 N1000 scale receipts`），包含聚合器、33 tests、三份聚合 artifact 与 Obsidian verified 记录。

## Final-scale Task 3 论文证据传播

- `script/build_v4_paper_evidence.py` 现在严格闭合总计、逐数据集和逐单元三层计时合同；任何 count/status/hash/gap 不一致都会把 scale 降级为 `partial`。
- Canonical `paper_evidence.json` 已绑定 aggregate SHA-256 `2ad8e3...16a95` 并返回 `scale.status=verified`；生成宏覆盖 15 units、225,000 proofs、0 failures、150 evaluation rounds、A/R 150,000/150,000、150 reconstructions、150 DP releases 和 timing mean/std。
- `script/check_heatmap_manuscript.py` 只在 verified 状态允许完整规模主张，并强制规模表、全部生成宏、Rome seed 101 / Synthetic seed 101 host-suspension 及 timing-only exclusion 披露。
- RQ6 已加入逐数据集 scale 表；Abstract 和 Conclusion 只声称可复现完整执行，并明确 4.183 h/单元不代表 interactive latency 或 optimized production throughput。
- Claim map 现为 18 supported、7 analytical、2 assumption、0 partial、0 unsupported；CE23 已由 strict aggregate 关闭。
- Manuscript checker 为 Abstract 147 words / 0 issues；builder、checker、summarizer 共 220 tests passed。Evidence builder 还会独立重算 overall/per-dataset timing mean 与样本标准差，并将未知 dataset 或统计篡改降级为 `partial`。下一步是 clean LaTeX/Biber build 和全页视觉检查。
- Task 3 已精确提交为 `3591866c`（`feat: bind verified V4 N1000 paper evidence`），包含 19 个 evidence builder、checker、manuscript、claim/self-review 与 Obsidian 路径。

## Final-scale Task 4 最终构建与视觉门禁

- 从 clean auxiliary state 完成 `pdflatex -> biber -> pdflatex -> pdflatex`；Biber 处理 90 citekeys，最终 PDF 为 18 页 US letter、PDF 1.7。
- 日志门禁：undefined/fatal/rerun 0、overfull 0、LaTeX/package warnings 0、Biber warnings/errors 0；42 个 underfull 经逐页视觉检查未形成可见缺陷。
- 18/18 页已渲染检查。RQ6 scale table 后加入 `FloatBarrier` 修正浮动体与解释段落顺序；修正后第 1--13 页像素不变，第 14--18 页按原始细节复查，无裁切、重叠、缺图或不可读表格/正文。
- 最终 SHA-256：`main.tex` `1e3a95d6...ef9ed`，`reference.bib` `c1c4d078...b6cf`，`main.pdf` `2e59d0cb...f3ca5`；canonical 与 Heatmap paper mirror 的 source/reference 字节一致。
- Evidence builder、manuscript checker 与 strict summarizer 共 220 tests passed；manuscript checker 返回 Abstract 147 words / 0 issues。
- 最终 verified-paper 产物已精确提交为 `71b7e418`（`docs: verify final-scale SHTPC TDSC manuscript`），未带入工作区其他变更。
- 实验和机器可验证论文门禁已全部闭合；仅剩作者 license、署名/单位/ORCID、COI、prior versions、funding、ethics、code/data availability、AI disclosure 与投稿系统选项。

## 关联

- [[00-Hub]]
- [[01-Plan]]
- [[Writing/论文修订与投稿决策]]
- [[Experiments/V4-Docker-N1000完整实验]]
- [[Results/Reports/V4实验结果-2026-07]]
