---
type: writing
project: tsip-heatmap
updated: 2026-07-14
status: active
tags: [tsip-heatmap, writing, tdsc, decisions]
---

# 论文修订与投稿决策

## 已形成的稳定决策

- 2026-07-13 确认 IEEE TDSC 为唯一投稿目标，采用分阶段证据驱动方案 A。
- 修订设计事实源为 `docs/superpowers/specs/2026-07-13-tsip-heatmap-tdsc-revision-design.md`，提交 `c6243a02`。
- 实施分为 `docs/superpowers/plans/2026-07-13-tsip-heatmap-tdsc-pre-scale-revision.md` 与 `docs/superpowers/plans/2026-07-13-tsip-heatmap-tdsc-final-scale-handoff.md`，计划提交为 `ed4d2f86`。
- pre-scale 可先完成所有不依赖最终两个 scale receipts 的修订；final-scale 只有在逐单元严格汇总为 verified 后才能更新 Abstract、RQ6 与 Conclusion 的 15/15 结论。
- `script/check_heatmap_manuscript.py` 是正文证据合同门禁：强制 Abstract 100--200 词、manifest-bound V4 macros、五数据集覆盖和 partial-scale claim boundary；当前未修订稿应以 11 issues/exit 1 失败，不能手工绕过。
- 当前权威稿件以仓库活跃 `TSIP/main.tex` 为准；`main_no_appendix.tex` 的 16 到 18 页记录属于阶段快照。
- 主题术语收敛到 SHTPC 和 proof-carrying admission layer，避免用 trajectory integrity、defense 或完整 robustness 描述有限准入关系。
- 核心 insight 是 trajectory continuity 与 payload validity 必须在同一个 ZK admission relation 中绑定。
- `Stateful-ContextCommit` 是 measured composition harness，不是完整 VDAF 或已有 SOTA protocol。
- Related Work 按 private location analytics、private aggregation/input validation、location authenticity/private accounting、ZK/circuit binding 分类。
- Evaluation 使用 RQ 结构区分 integrity enforcement、residual/in-envelope risks、privacy/utility、cost/scalability；后续 V4 结果继续保持证据分层。
- 不删除仍被正文使用的参考文献；2026-06-30 citation/ref audit 和 2026-07-06 bibliography audit 均通过。

## 历史修改结果

- 2026-06-18：无附录稿 18 页，摘要约 300 词、supplementary results 和零页数余量被识别为投稿风险。
- 2026-07-05：删除 `Detailed Evaluation Results` 后无附录稿降至 17 页；继续压缩后曾达到 16 页且引用完整。
- 2026-07-05 至 07-07：重构 RQ、摘要、Introduction、Related Work、Method motivation、theorem、Discussion 和 Conclusion；多次编译无 undefined citation/reference。
- 2026-07-06：一次扩展使稿件达到 22 页，随后按用户要求收缩强主张。该 22 页状态不是当前可引用页数。
- 2026-07-07：活跃 `main.tex` 的若干检查点为 16 或 17 页、87 citekeys；实际投稿前必须重新编译确认当前页数。
- 2026-07-09：从两数据集扩展到 T-Drive、GeoLife、Porto、Rome 和 Synthetic，解决数据集覆盖不足，但 Synthetic 必须明确标为合成集。

## 投稿前仍需确认

- 当前标题最终采用 TSIP 还是 SHTPC 命名，以及内部 labels 是否全部同步。
- Abstract 在 final-scale 更新后为 147 词并通过 100 到 200 词门禁；manuscript checker issues 为 0。
- 当前 verified pre-scale PDF 为 18 页；author biographies、supplementary files 和匿名选项仍待作者确认。
- 数据许可、AI disclosure、code/data availability 和 COI 声明。
- 最终 clean PDF 页数、全页视觉 QA 和 canonical/paper mirror 字节一致性。

## 2026-07-13 当前稿审计

- 活跃标题已是 `SHTPC: Stateful Hidden Trajectory-Payload Consistency for Private Heatmap Aggregation`，可见术语以 SHTPC 为主；仍有内部 label `eq:r-tsip` 待清理。
- Abstract 约 247 词，高于 IEEE Computer Society regular-paper 的 100 至 200 词范围。
- `TSIP/main.pdf` 已于 2026-07-13 从 clean auxiliary state 重新构建为 18 页，覆盖五数据集固定效用、V4 artifact 分层、合规和引用修订；最终 scale aggregate 后仍需再次构建。
- 当前 Evaluation 仍写 T-Drive、GeoLife 加 derived subsets，headline 为 N=50，尚未接入四个真实数据集加一个 Synthetic 的固定公平结果。
- `TSIP/main.tex` 的 staged deletion/untracked replacement 风险已由保全提交 `2636cec8` 关闭；它仍是唯一 canonical manuscript，isolated paper 只在最终构建通过后同步。
- 旧 3,056/14/23 当前电路主张已在 Task 4 清除；当前 R1CS 数字只通过生成宏引用 3,547/16/25，历史 3,056/14/23 receipt 在 RQ6 独立标注且不提供当前性能结论。
- 当前 manifest 没有绑定 serializer size receipt，因此 5,694 B frame 主张已删除；128 B 只保留为 historical performance receipt。当前 circuit latency/throughput 已在 final-scale Task 3 由 strict 15-unit aggregate 关闭。
- 已建立 `DATA_NOTICE.md`、Heatmap artifact index、citation audit 与作者确认清单；仓库仍无 LICENSE，必须等待作者明确选择 SPDX license，AI、COI、prior-version、funding、ORCID 与 ethics determination 均保持未确认。

## 禁止沿用的过期判断

- “只有两个数据集”：已被五数据集实验替代。
- “paper-scale V4 尚未启动”：启动前报告中的旧状态。
- “15/15 launcher pass 已等于论文级性能结论”：launcher 从来不单独支撑性能；当前论文数字来自后续 strict aggregate 与 hash-bound paper evidence。
- “Docker healthy 等于实验仍在跑”：当前中断案例已证明不成立。

## 2026-07-13 Task 5 写作合同

- Abstract 当前为 155 词、五消息结构；scale partial 明确不进入 Abstract。
- Abstract evidence 只绑定 22/22 static 与 fixed fair five-dataset utility；“五数据集均为正增益”同时披露两项增益很小，不写成 uniformly large。
- `TSIP/CLAIM_EVIDENCE_MAP.md` 是 claim 审计事实源：27 个 major claims、Task 6 后 89 个段落级 reverse-outline 条目、24 个 reviewer checks；保留 claim 中 `unsupported=0`。
- Task 9 后 citation/artifact 两项已关闭；唯一 `partial` 是 current N=1000 latency/throughput，final-scale strict aggregate 关闭前不得写成 paper-ready。

## 2026-07-13 Task 6 五数据集固定证据合同

- Experimental Setup 已逐段分离 original-input utility、`TSIP_SAFE_PROTOCOL_TRAJECTORY=1` clean protocol smoke 与 full Groth16 scale；safe trajectory 明确不是 RQ5 utility 来源。
- RQ5 固定 `epsilon=5.0`、post-admission release `tau=2.0`、`N=1000`、`T=10`、10% A1、top-50 Jaccard 与 seeds 101/202/303，不使用逐数据集 tuned setting。
- SHTPC/Nebula Jaccard 与绝对增益为：T-Drive 0.671/0.561（+0.110）、GeoLife 0.878/0.366（+0.512）、Porto 0.305/0.298（+0.007）、Rome 0.772/0.664（+0.108）、Synthetic 0.135/0.128（+0.007）；Porto 与 Synthetic 明确写作 small gains。
- 生成表 `TSIP/tables/tab_v4_fixed_utility.tex` 与 canonical 图 `TSIP/figures/v4_fixed_utility_heatmap.pdf` 均来自 fixed receipt；图生成器对数据集顺序、参数、baseline、gate、数值范围与 delta fail closed。
- 图像连续两次生成的 PDF SHA-256 均为 `e0efc93e91a2f7dd128ce0552eb930470f724ef9c92052ca9294ee6293ec5915`；PDF 一页且十个 cell 文本值完整。
- 手稿合同为 155-word Abstract、0 issues；focused 169 passed；unaffected settlement tests 28 passed。全局默认 pytest 因并发的非 Heatmap charger policy 工作区缺少 `ruc-demo` profile 而 11 failed/34 passed，此漂移未被本任务修改或隐藏。
- Task 6 提交为 `3f1d5d4b`；仅包含 canonical manuscript/map/figure、deterministic generator 与 10 项 generator tests，未提交分析目录重复导出。

## 2026-07-13 Task 7 合规与引用合同

- `DATA_NOTICE.md` 是五数据集发布边界事实源；四个真实数据集不发布 raw 或 row-level prepared trajectories，只发布预处理代码、非行级 manifest 和聚合证据。
- T-Drive 与 GeoLife 的 Microsoft Research License Agreement 禁止数据及其衍生物再分发；GeoLife 官方 1.3 指南要求三篇引用，T-Drive 指南要求两篇。Porto 为 CC BY 4.0；Rome 依 CRAWDAD 指引引用贡献者、禁止去匿名化并引导用户回到官方源。
- `TSIP/CITATION_AUDIT.md` 使用 publisher/standards body/official dataset owner/original paper 作主来源；修正 Prochlo 十作者、GeoLife 三作者与 Poplar DOI，新增 T-Drive/GeoLife 必引文献及 Porto/Rome 官方记录。
- Artifact/Reproducibility 与 Ethics 已明确：15/15 launcher receipt 不是 verified scale aggregate；开发 Groth16 setup 不是 production ceremony；模拟 reviewer reports 仅为内部质量控制；真实轨迹二次使用的机构 ethics determination 由作者确认，不在工件中猜测。
- 代码许可仍为 `BLOCKED: explicit author license selection required`，因此未创建 `LICENSE`。作者顺序、ORCID、通讯邮箱、COI、prior/concurrent versions、funding、ethics、code/data wording 与 AI disclosure 均在投稿清单中保持未勾选。
- Task 7 检查点门禁：Abstract 155 words/0 manuscript issues，focused 159 passed，五数据集字段计数均为 5，raw/prepared Git tracked count 为 0，whitespace check 为 0。
- Task 7 已提交为 `dec09503`，仅包含 7 个合规/稿件路径；参考文献系统性修订必须通过 Task 8 Biber、undefined citation/reference 与逐页 PDF 门禁后再进入 verified-paper 提交。

## 2026-07-13 Task 8 构建合同

- clean build 使用 `pdflatex -> biber -> pdflatex -> pdflatex`，Biber 处理 90 citekeys；undefined/fatal/rerun 与 Biber WARN/ERROR 均为 0。
- 最终 PDF 为 18 页 letter，SHA-256 `376273f598eb5c2050caae759b5a5d9e184d465fa1066edadf3c5f4e8e9f6888`；18/18 页已逐页渲染检查，无裁切、重叠、缺图或不可读表格。
- 日志只有 42 个 underfull 提示，overfull/LaTeX/package warning 均为 0；视觉检查确认 underfull 未造成可见缺陷。
- canonical 和 Heatmap paper mirror 已字节一致：`main.tex` SHA-256 `5fd63e...fea4`，`reference.bib` SHA-256 `c1c4d0...b6cf`。
- 本机 BasicTeX 复现需要用户级 Courier package；未安装时 `times` 的 `\texttt` 会缺 `pcrr7t.tfm`。该依赖由 `tlmgr --usermode install courier` 解决。
- 当前 18 页是 pre-scale verified build，不含 strict aggregate latency/throughput；最终-scale 写回后必须重新执行同一整套 build/visual gate。
- Task 8 已以精确路径提交为 `d54ac1a5`（`docs: verify pre-scale SHTPC TDSC manuscript`）；该提交是 final-scale 写回前的可追溯论文基线。

## 2026-07-14 Task 9 对抗自审合同

- 五维自审不把 simulated reviewer output 当作同行评议；它只是内部 claim/evidence 质量门。
- 27 项 major claims 收敛为 17 supported、7 analytical、2 assumption、1 partial、0 unsupported。CE23 为唯一 partial，且未进入 Abstract。
- 审查发现并删除 MDS/EETS 可支撑 HBC/非合谋的过强类比；新文本将组织分离、访问文档和跨角色审计写成显式部署假设，并给出单操作者可关联角色时的失效条件。
- 五数据集 x 三种子的 15 个固定配置对均为正增益，但绝对 Jaccard 与小增益仍完整披露，不使用 uniformly high/large 措辞。
- 审查后 clean PDF 仍为 18 页，`main.tex` SHA-256 `77e57e...e12`，`main.pdf` SHA-256 `b28bc8...fcb`；169 focused tests 通过。
- 最终规模写回必须逐单元验证 16 rounds、proof generation/verification、A/R 1000/1000、reconstruction、DP 与 manifest/launch/unit hashes，生成 JSON/CSV/LaTeX 后才能修改 RQ6。
- Task 9 提交为 `a0db10db`；后续论文变化必须相对该 pre-scale review checkpoint 说明 strict-scale 证据增量。

## 2026-07-14 Final-scale Task 3 写作传播合同

- `paper_evidence.json` 已绑定 strict aggregate SHA-256 `2ad8e35340d0646c3b0e130468f9a00c5b66e433bedf4411929fe9f166516a95`，scale 状态为 `verified`。
- Abstract 为 147 词，保留五消息结构；规模句只报告 15 个 N=1000 units、225,000 verified proofs 与 0 failures，不把 prototype runtime 写成低延迟优势。
- Experimental Setup 定义 7,200 s timing eligibility 与每数据集至少两个 timing units；RQ6 将 15 个功能 receipts 与 13 个 timing receipts 分开报告。
- Rome seed 101 与 Synthetic seed 101 因 host suspension 只排除 timing；二者仍进入 proof、route、reconstruction 和 DP 功能总账。
- 规模表和正文数字均来自生成 artifact/macros；checker 强制表、14 个 scale macros、两个排除 pair 和 timing-only disclosure。
- CE23 已从 `partial` 升级为 `supported`；27 claims 现为 18 supported、7 analytical、2 assumption、0 partial、0 unsupported。
- `check_heatmap_manuscript.py` 返回 Abstract 147 words / 0 issues；evidence builder、manuscript checker 与 summarizer 共 220 tests passed。Builder 独立重算 overall/per-dataset timing mean 与样本标准差，拒绝未知矩阵成员和统计篡改。
- 结果解释边界：1.000 +/- 0.069 proofs/s、4.183 +/- 0.248 h/单元支持 reproducible full-matrix execution，不支持 interactive latency 或 optimized production throughput。
- 下一门禁为 clean LaTeX/Biber rebuild、warning scan、全页视觉 QA 和 paper mirror 同步；license 与投稿声明仍由作者确认。
