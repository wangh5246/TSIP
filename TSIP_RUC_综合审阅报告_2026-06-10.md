# TSIP-RUC 综合审阅报告

> 审阅日期：2026-06-10
> 审阅对象：`TSIP_RUC_完整设计文档_2026-06-10.md`（主设计文档）、`handoff_e1_tier1_2026-06-10.md`（进度文档）、仓库实现代码与实验产物
> 审阅方法：所有结论均对照实际代码/CSV/编译产物核验，非仅读文档

---

## 1. 总体判断

设计文档质量很高：口径诚实、数字可追溯、claim 措辞谨慎（如 E1 的"两半分句"写法、Rome 保守下界、明确区分"已实测/已实现未跑/计划"）。这是能扛住 artifact evaluation 的文档风格，应保持。

当前项目状态：**E1/E2/E6 有可引用结果，E3/E4/E5 是论文成立与否的关键缺口**。最大的两个风险不在实验，而在：(a) 隐私提交路径未落地导致 privacy claim 悬空；(b) 信任假设的"why zk at all"问题没有正面回答。

---

## 2. 实现与文档一致性核对（已逐项验证）

### 2.1 已验证一致 ✅

| 项 | 核验结果 |
|---|---|
| E1 GeoLife 主表数字 | 与 `experiments/e1_objective_fix/geolife_summary.csv` 逐位一致（0.2696/0.2765/0.40；lifted 0.0837/0.0849/0.1417；pinned 全 0） |
| E2 阈值表数字 | 与 `tab_e2_incentive_thresholds.csv` 一致（controlled_120s: alpha 0.6 / penalty median 0.431 / p95 0.580 等） |
| E2 全套 artifacts | 21 个文件齐全，含 forensic rows |
| E3 scaffold | `common/eval_experiments.py` 中 `e3_residual_sweep()` / `e3_soundness_check()` 确实存在 |
| charger 隐私差距 | `services/charger/app.py` 第 121 行 `fixes: list[...] = Field(min_length=2)` 确认仍接收 raw fixes |
| compiled artifact 21 signals | `verification_key.json` `nPublic=21`；`.sym` 文件前 21 个信号正是 15 个基础信号 + `max_dt_sec` + 5 个 time/month 信号 |

### 2.2 P0 #1 的 provenance 已查清（可直接行动）❗

设计文档说"需确认 artifact 来源"。**已确认**：

- `zk/settlement_period_v5_k6/` 编译于 6 月 2 日，`.sym` 显示编译时 wrapper 列了 21 个 public 信号 → **artifact 是正确的新版本**。
- `circuits/settlement_period_v5_k6.circom` 当前只列 15 个，且该文件的唯一 git 历史是 `8a19a545 "recover: restore emptied working-tree files from dangling git objects"` → **源文件在一次 git 事故后被恢复成了旧版本**，artifact 比源码新。

修复路径明确，半天可完成：

1. 给 `k6.circom` 的 public 列表补上 `max_dt_sec, period_start_time, period_end_time, month_id, month_start_time, month_end_time`（顺序按 `.sym`），同步头部注释到 21 项。
2. 重编译一次，diff 新旧 `.r1cs`/vkey 哈希确认等价（或至少 nPublic 与 signal 顺序一致）。
3. 加回归测试：wrapper public list、`prove_settlement_period_v5.py` 输出 count、charger `_validate_zk_time_signals` 的 index 布局三者一致，任一漂移即 fail。

### 2.3 两份文档互相冲突（需清理，否则新会话会被误导）❗

handoff_e1_tier1 有三处与代码/设计文档矛盾，**均以设计文档和代码为准**：

| handoff 说法 | 实际情况 |
|---|---|
| §1 "`GRID_SIDE=6`，cell_x/y 范围 0..5，超出 WASM 拒收" | base circuit 第三参数是 `K_WINDOW=6`（continuity 窗口），`GRID_W=100`，cell range check 对 100。handoff 自己 §2.5 也写 GRID_W=100，自相矛盾 |
| §10 "E6 已有真实数据：rapidsnark ~320ms，3056 constraints，128-byte proof" | 3056 constraints 应是旧 TSIP 聚合电路的数字。当前电路 226,394 constraints，snarkjs prove ~7.1s，proof 805 bytes |
| §10 "五个计划实验组 + 7 baseline（RiseFL/Nebula/EIFFeL...）+ Jaccard/RMSE utility 待做" | 这是 RUC pivot 之前老 TSIP 聚合论文的实验计划，与 E1–E6 框架冲突 |
| §10 "E2 是 scaffolding 未正式跑" | E2 已完成增强版（handoff 比设计文档早 6 小时，已过期） |

**建议**：在 handoff 顶部加一行 superseded banner（"电路参数、E6 数字、实验计划以 TSIP_RUC_完整设计文档 为准"），不要删内容（符合你的文档维护规则 2）。

### 2.4 仓库卫生风险 ❗

`git status` 显示大量 deleted/modified 未提交，且已发生过一次"工作树文件被清空、靠 dangling objects 恢复"的事故——P0 #1 正是这次事故的直接后果。**建议立即做一次干净 commit**（包括 zk artifact 的哈希记录或 LFS/重现脚本），否则下一次事故可能丢的是无法重编译复现的 `.zkey`（trusted setup 产物，130MB）。

---

## 3. 客观性审阅（reviewer 视角）

### 3.1 最强攻击点："why zk at all?"（必须正面回答）

Threat model 同时假设 trusted receiver、trusted odometer、OSNMA、设备 attestation。Reviewer 必然问：**既然 receiver 已经被信任到能产生认证 fix 并 attest root，为什么不直接让它本地算好账单签名上传，省掉 7 秒的 SNARK？**

这个问题目前文档没有回答。可行的答案是信任最小化分层：receiver 只被信任做一件窄事——为原始传感读数签名/attest；**计费逻辑、tariff 版本绑定、月度窗口、轨迹物理可行性约束全部不依赖 receiver 固件正确性**，由 proof 承担。这样 tariff 更新不需要 OTA 信任、receiver 固件 bug/篡改不能改账单、charger 不需要审计每台设备的计费代码。建议把这段写进 §2 threat model 和 intro，作为 design rationale 的一等公民，而不是等 rebuttal 再说。

### 3.2 "first" claim 风险

> "TSIP-RUC is the first RUC design point that jointly supports zone-based charging, strong route privacy, and zero roadside enforcement infrastructure..."

"first" 需要彻底的 related work 排查。至少要核对：P4TC（provably-secure toll collection）、PriPAYD/PriPAYD-style pay-as-you-drive、基于 TEE 的 RUC 方案、以及 2024–2026 年间可能出现的 zk-based tolling 工作。建议措辞改为 "to our knowledge, the first ... in the design space of [table 1.3]"，并把那张设计空间表作为支撑。三轴定位本身是好的，"first" 是否成立取决于第四轴（trusted receiver 假设）是否被算进对比。

### 3.3 E1 的统计脆弱性

GeoLife n=14 作为 economic anchor，文档已正确定位为"机制演示非统计估计"，且 lifted/zone-binding 数字经过 objective-fix 逐位封印，这部分是扎实的。但仍有两点要补：

- 主表只在 `distance_bucket=25m, neighbor_radius=2` 单配置下报数。sweep 已定论不进论文，但**表注必须写明配置取值及理由**，否则 reviewer 会问 cherry-picking。
- GeoLife 14 个 period 的 device 集中度（来自几个用户）必须如实写进 limitation——handoff 决策 3 已有此意，论文里别漏。

### 3.4 E6 数字的引用边界

- snarkjs 7.1s 是本机实测，可引用；**rapidsnark ~0.6s 是外推**，月度成本表（19s/月等）全部建立在外推上。要么实测 rapidsnark 一次（工作量小、收益大），要么表里明确标 "extrapolated"。
- verify ~223ms/period，百万车辆×30 period/月 ≈ 单核 77 天/月，规模化需要并行/批验证讨论。direct accumulation 叙事在单车视角成立，**charger 侧聚合吞吐**应在 E6 或 discussion 里给一句量化。

### 3.5 E2 残留问题

- GeoLife `all_outage`/`moving_outage` 行是 `no_penalty_observations`（GeoLife 无 native gap，只有 controlled outage 有数据）。这本身合理，但文档 §11 没解释，论文表里出现空行会被问。建议表注一句话说明 GeoLife native cadence 太密无自然 outage。
- E2 的 incentive 结论依赖 `speed_bound_ok`；建议在论文里报告违反速度上界的 interval 比例（如果为 0 更好，写出来）。

### 3.6 隐私 claim 的悬空状态

这是论文成败级问题。当前所有隐私论述都是"目标协议应当如此"，而唯一实现的提交路径上传 raw fixes。**只要 proof-only path 没有最小实现，E5 就没有测量对象，privacy claim 就只是设计意图**。NDSS/PETS reviewer 对"我们原型测 correctness、隐私是另一条没实现的路径"接受度很低。最低限度：charger 加一个 proof-only endpoint（statement + proof + attestation-over-root），negative tests 证明它拒绝坏 statement，E5 以它为对象。

---

## 4. 可实现性评估与工作量排序

| 任务 | 难度 | 预估 | 备注 |
|---|---|---|---|
| P0-1 wrapper 信号同步 + 回归 | 低 | 0.5 天 | provenance 已查清（§2.2），照做即可 |
| git 仓库清理 commit | 低 | 0.5 天 | 防止再丢 trusted setup 产物 |
| P0-2 proof-only 提交路径 | 中 | 2–4 天 | 不要试图把 Ed25519 验签塞进电路（circom 内成本不可接受）；务实路径是 receiver attest `receiver_fix_root`，charger 验 attestation + proof。threat model 文字同步改 |
| E3 residual sweep | 中 | 1–2 周 | solver/hook 已在，主要是 runner 把 relay radius × granularity × cadence 串起来 + forensic CSV。`payload_rem` 路径若工作量大，可先只做 cell-level 并如实标注保守下界 |
| rapidsnark 实测 | 低 | 1 天 | 把 E6 外推变实测，性价比最高的小任务 |
| E4 spot-check baseline | 中高 | 2–3 周 | **不要手写 PBF parser**，用 osmnx/pyrosm 直接拿 drivable graph，工作量可砍一半以上。模型本身（P(caught) 反解摄像头密度）是闭式计算，难点全在路网/overlay 对齐 |
| E5 leakage 最小版 | 中 | 1–2 周 | 依赖 P0-2。最小版本：GPS-upload baseline 的 stay-point/重识别（现成方法），vs proof-only statement 的 (fee, distance, fallback, period window) 元组推断攻击 + cadence 敏感性 |
| sparse tariff 电路 | 高 | 不做 | 维持 future work，正确决策 |

**结论：文档 §19.1 最短路线整体正确**，修订两点：(1) 把"rapidsnark 实测"插进去（1 天换掉所有外推数字）；(2) P0-2 的优先级应高于 E3——E3 缺了是结果不全，P0-2 缺了是 privacy claim 不成立，性质不同。

---

## 5. 论文写作角度的额外建议

1. **handoff §8 的两个 framing finding 价值很高**，尤其"约束的边际经济价值是 tariff 空间结构的函数"（GeoLife 0.27 vs Rome 0.0056 的对比）——这是 E1 从"消融表"升级为"有分析深度"的关键一段，建议正文给一小节而非脚注。
2. `main.tex`（4 月 24 日，85KB）很可能还是 RUC pivot 前的老 TSIP 聚合论文骨架。开写前先审一遍能复用多少，避免在旧叙事上缝补。
3. soundness 修复（max_dt/bitwidth，§6.4）写 security analysis 是对的；建议同时在 artifact 里留一个"修复前电路可被绕过"的最小 PoC 测试，这类 negative evidence 在 NDSS artifact evaluation 很加分。
4. E2 主叙事（GeoLife clean anchor + Rome diagnostic upper bound，不混 absolute rate 曲线）是对的，保持。
5. 投稿目标如仍是 NDSS 2027 Cycle 2 / PETS 2027：以当前缺口（P0-2 + E3 + E4 + E5 + 正文未动笔），强论文路线时间相当紧，建议按"最短可发表路线 + E4"规划，E5 做最小版。

---

## 6. 行动清单（按序）

1. ☐ 修 `k6.circom` public 列表（21 信号）+ 三方一致性回归测试（§2.2 给了精确步骤）
2. ☐ git 干净 commit + zk artifact 哈希/重现记录
3. ☐ handoff_e1_tier1 顶部加 superseded banner（§2.3 三处错误）
4. ☐ charger proof-only endpoint + threat model "why zk" 段落
5. ☐ rapidsnark 实测，替换 E6 外推
6. ☐ E3 residual runner（relay × granularity × cadence）
7. ☐ E4 用 osmnx 起步，三城摄像头密度表
8. ☐ E5 最小 leakage 量化（以 proof-only statement 为对象）
9. ☐ related work 排查 "first" claim（P4TC、PriPAYD、TEE-based RUC、近两年 zk tolling）
10. ☐ 审 `main.tex` 可复用性，落 handoff §8 两个 framing finding
