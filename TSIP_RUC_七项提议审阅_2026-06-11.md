# TSIP-RUC 七项提议逐项审阅

> 日期：2026-06-11
> 审阅对象：任务定义正式化 / 主基线三件套 / fraud-centric metrics / 按周期增量证明 / 可信 OBD-II 与防拆 / 跨辖区互操作与双模式 / 组合侧信息泄漏
> 数据核查：`dataset/osm/` 已有三城路网 PBF（beijing 89M、portugal 395M、italy-centro 361M）+ Rome ZTL GeoJSON，E4/组合泄漏分析的路网输入已就位。

## 0. 决策总表

| # | 提议 | 裁定 | 形态 |
|---|---|---|---|
| 1 | 任务定义正式化 | **做，高优先级** | 写作任务，论文骨架 |
| 2 | 主基线三件套 R/T/Z | **做**（已设计，按 R>T>Z） | R 实现，T/Z 分析 |
| 3 | fraud-centric metrics | **加一层，不替换** | 在 savings_ratio 之上加呈现层 |
| 4 | 按周期增量证明 | **不进主线**；做构造草图 + 可选微基准 | future work 升级版 |
| 5 | OBD-II/见证设备/防拆 | **写映射，不做设计** | deployment 小节 |
| 6 | 跨辖区 + 双模式 | **双模式做（近乎免费）；跨辖区一小节；完整互操作不做** | 协议泛化小节 |
| 7 | 组合侧信息泄漏 | **做，且应直接成为 E5 的正式定义** | 实验，复用 E1/E3 solver |

核心纪律：七项中只有 #2(R) 和 #7 是新实验，其余全是写作/分析。**任何一项都不应触碰电路重编译**（#4 的微基准除外且可选）。P0 两项（wrapper 信号一致性、proof-only path）仍然排在所有七项之前。

---

## 1. 任务定义正式化 —— 做，这是论文骨架

当前设计文档的 security claims 是散文式的（§17.2），NDSS/PETS 需要正式语法和定义。建议结构：

**协议语法**（algorithm 元组）：

```
Setup(1^λ, tariff) → (pp, tariff_root)
Enroll(device) → (attestation_commitment, registry entry)
CollectFix(...) → authenticated fix
ProvePeriod(fixes, tariff, pp) → (statement, π)
VerifyPeriod(statement, π, registry, calendar) → accept/reject
AggregateMonth(accepted statements) → bill
Dispute(statement, indices) → selective opening   ← 创新补充§2.5的仲裁协议自然入列
```

**两个安全定义**，各自挂接你已有的东西：

- **Billing soundness（game-based）**：对手赢当且仅当 VerifyPeriod 接受某 statement，而不存在满足全部物理/tariff/时间约束、且按 `fee_for_interval_values` 计费等于 statement 总额的 witness。二元版本由 Groth16 knowledge soundness + 电路约束给出；**量化版本即 ε-economic soundness**（创新补充 §2.1）——正式化让 ε 有了 game 语义，E1/E3 是它的实测。
- **Privacy（leakage-profile 式）**：显式定义泄漏函数 `L(trajectory) = (fee, distance, fallback_count, period/month window, roots)`，claim 为"charger 的 view 可由 L 模拟"。**把 L 写成显式对象是关键一步**：它使 #7 的侧信息分析有了攻击目标（攻击的就是 L 的反演），也使"我们不声称 L 本身无泄漏"成为定义内的诚实声明而非补丁。

注意一致性：正式化必须以 proof-only path 为对象（P0-2 的又一个依赖方）；fallback/outage 声明要进语法（ProvePeriod 的输入含 outage declaration），否则 E2 在形式框架外悬空。工作量 1–2 周写作，无实验风险，建议与 P0-2 并行启动。

## 2. 主基线三件套 —— 维持前一文档结论

R（现代重建 spot-check）> T（TEE 对比）> Z（ZKLP 谓词分析）。补充一点：OSM PBF 已就位意味着 R 的 spot-check 布点模拟和 E4 共享同一路网加载器——**先写路网 loader（osmnx/pyrosm），R 和 E4 同时解锁**，这是当前依赖图上的关键节点。

## 3. fraud-centric metrics —— 加一层，绝不替换底层

你问了两次，说明在认真考虑替换。**明确建议：不要替换 savings_ratio。** 它已逐位封印、有 receipt 链、是机制实验的正确原语；改测量原语 = 已封印结果全部作废重跑，且会破坏"E1 是机制演示非统计估计"的既定口径。

正确做法是在它之上加**派生呈现层**（零重跑，纯计算）：

| 新指标 | 定义 | 来源 |
|---|---|---|
| Revenue leakage rate | 给定对手人群假设下运营方月收入流失比例 = Σ(ε × fee) / Σfee | E1 的 ε 分布 × 车队规模假设 |
| Detection outcome per attack class | proof-violating 攻击：确定性拒绝（TPR=1, 即时）；proof-consistent 残差：E3 的 ε | E1/E3 + Baseline R 的概率检出对照 |
| Honest-user cost（FPR 的经济类比） | 诚实用户因 outage 被多收的费用分布 | E2 penalty 已有 |
| Detection latency | TSIP：提交时；spot-check：审计周期 | 与 Baseline R 对照 |

这层的价值：给政策/运营读者一个钱的语言（"每月每万辆车流失 X 美元"），同时让与 Baseline R 的对比有共同度量（spot-check 的 TPR<1、有延迟、有误指控成本 vs 你的确定性即时拒绝、误差代价=fallback 多收费且有界）。**fraud-centric 不是换指标，是把 ε 翻译成欺诈经济学的语言。**

## 4. 按周期增量证明 —— 不进主线，写构造草图

判定理由：

- 当前 per-period Groth16 在运营上已自洽（后台生成 + direct accumulation，E6 有数）；切换到 IVC/folding 是**整个证明栈重写**（circom → folding-friendly frontend），E6 全部作废，数周到数月，且与论文贡献正交——你的贡献不在证明系统工程。
- 这有成为第二个 "N_FIXES sweep" 弯路的风险（handoff 决策 7 的教训：不要在主线上重开高成本低增益的工程探索）。

但要把 future work 写到"已设计"的深度：电路本身已是 per-interval 步进结构（每段约束同构），天然适配 Nova 式 IVC——每个新 fix fold 一步，period 收尾输出常数大小证明，prove 成本摊进驾驶过程。写出步函数定义 + 状态向量（running fee/distance/fallback accumulator + 上一 fix 承诺）+ 引用 Nova，半页即可。**可选加分**（时间充裕才做）：用 Nova 实现单个 interval 步函数跑一行微基准，证明方向可行，1–2 周，不动主线电路。

## 5. OBD-II/见证设备/防拆 —— 写假设到硬件的映射，不发明机制

三个子问题分开裁定：

- **OBD-II 作为 odometer 来源：写**。真实 RUC（OReGO 等）正是用 OBD-II dongle 读里程；欧盟 Smart Tacho 2 自带认证里程。加一个 deployment considerations 小节，把协议的每条信任假设映射到在售硬件：odometer ← OBD-II/tachograph motion sensor；authenticated time/position ← OSNMA receiver；attestation ← 车规 SE/TEE 的 key。**这是把 threat model 从公理变成采购清单**，对 NDSS 读者价值很高，纯写作。
- **防拆策略：不做设计**。物理防拆是硬件工程，不是这篇论文的贡献面；自己发明防拆机制只会制造新攻击面。正确做法：引用 smart tachograph 法规已有的防拆体系（运动传感器配对、铅封、定期校准、tamper-event 记录），声明"防拆由该法规体系提供，本协议消费其输出"。E1 已把 odometer 完整性归为 attestation 边界（handoff 决策 4），此处保持同一口径。
- **见证设备（第二设备交叉佐证）：不进本文**。它改变 threat model 拓扑（两设备合谋分析、配对协议），是一篇独立论文的体量。一句 future work 占坑即可。

## 6. 跨辖区互操作与 GPS/non-GPS 双模式 —— 双模式是隐藏的免费贡献

**双模式：强烈建议做，因为它几乎已经存在于你的协议里。** 关键观察：

> 全 period 声明 outage = odometer-only 计费（fallback distance × fallback rate）。**你的 fallback 机制本身就是 non-GPS 模式**：用户选择不出示任何位置，按速度上界距离 × alpha·max_rate 付费。E2 的 `all_outage` penalty 数据**就是这个隐私档位的价格表**。

这把三件已有的东西统一成一个新贡献：privacy-tiered pricing menu——

| 档位 | 用户出示 | 计费 | 隐私 | 价格（已有数据） |
|---|---|---|---|---|
| Zone-priced | proof + statement | 精确 zone 计费 | period 元组 | 基准 |
| Odometer-only（non-GPS） | 全 period fallback | vmax 距离 × alpha·max_rate | 零位置 | E2 all_outage penalty |
| 混合 | 部分 interval outage | 逐段混合 | 介于两者 | E2 controlled penalty |

现实锚点：OReGO 真实提供 GPS 与 non-GPS 选项——你的协议在密码学层面统一了这个菜单，且 E2 给出了档位间的精确价差和激励相容边界。写一节，零新实验，把 E2 从"outage 实验"升格为"隐私分层定价机制"。这是七项提议里性价比最高的一项。

**跨辖区：一小节展示组合性，不做完整互操作。** 协议天然支持多 authority：proof 绑定 `tariff_root + tariff_version`，每个辖区一个 root，车辆按辖区切分 period 分别证明、分别结算；月度 ledger 本就 per-charger。写半页"multi-authority composition"说明 + 指出 ZTL/拥堵区可作为同城第二 tariff 层（**`rome_ztl_centro_diurna.geojson` 已在库**，可直接做一个双层 tariff 的演示性 E1 行，顺便强化"约束价值依赖 tariff 拓扑"的 finding）。完整互操作（辖区间清算、漫游、EETS 合规）是政策工程，future work 一句话。

## 7. 组合侧信息泄漏 —— 做，并且就让它成为 E5 的正式定义

这是七项里研究价值最高的一项，且你有独家工具。当前 E5 计划（对比明文 GPS）太弱（前文已指出稻草人问题）；组合侧信息攻击才是诚实的隐私评估：

**攻击模型**：好奇的 charger 持有 L(trajectory) 序列（#1 正式化给出的泄漏函数输出）+ 公开侧信息：tariff 拓扑、**OSM 路网（已在库）**、土地利用、账单地址（计费关系固有泄漏）。问题：能把可行轨迹集合收缩到多小？

**关键的创新性复用**：E1/E3 的 RCSPP solver 枚举"约束一致的最便宜路径"；把目标函数从最小化费用改为**枚举与观测 statement 一致的全部可行路径**，同一套机器立刻变成隐私审计器——

> Our adversary solver doubles as our privacy auditor: the same constraint system that bounds what a cheater can claim also bounds what an observer can infer.

这句话是论文级的对偶观察：完整性约束越紧，statement 反演出的可行集越小——**soundness 和 privacy 在你的设计里是此消彼长的同一个几何对象**。量化输出建议：可行路径集大小/熵（anonymity set）随 (fee, distance, fallback) 元组的收缩曲线；cadence 60/120/300s 对收缩的影响（与 E6 性能轴耦合成同一旋钮）；以及 #6 双模式各档位的可行集对比（odometer-only 档的可行集大多少——隐私档位的收益被量化）。

范围纪律：侧信息限于公开数据 + 计费元数据，不做 ML 轨迹重构（那是另一篇论文）；依赖 P0-2（proof-only statement 定型）和 #1（L 的显式定义）。工作量 2–3 周，大部分是 solver 改造和路网对齐，路网文件已就位。

---

## 8. 修订后的总执行顺序

```
P0-1 wrapper 信号一致性（0.5天）
P0-2 proof-only path（2–4天）        ┐
#1 正式化（1–2周，写作，可并行）      ┤ 并行
#3 metrics 呈现层（2–3天，纯计算）    ┘
路网 loader（osmnx，解锁 E4+R）
#2 Baseline R（1–2周）→ E4 正式数字
#7 E5=组合侧信息（2–3周，复用 solver）
#6 双模式一节 + ZTL 双层 tariff 演示行（3–5天）
E3 residual sweep（既定计划）
#4 IVC 构造草图（半页）+ 可选微基准
#5 deployment 映射小节（1–2天）
```

与既定最短可发表路线完全兼容；七项裁定后新增的硬实验只有 Baseline R 和 E5 重定义两项，其余以写作吸收。论文不急，按此序走完，攻击面收敛点：任务有形式定义、指标有欺诈经济学语言、基线无年份问题、隐私有自我攻击、每条信任假设有在售硬件对应、双模式和仲裁是免费的部署完备性——这已经是一篇按"被攻击点尽可能少"标准构造的论文。
