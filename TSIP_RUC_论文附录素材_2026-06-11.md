# TSIP-RUC 论文附录素材

> 日期:2026-06-11。对应 `TSIP_RUC_完整设计文档_2026-06-10.md` §21.4 #19。
> 四份素材各自给出:appendix 定位、来源 artifact、可直接改写成英文附录的叙述要点。所有数字均有来源文件,不手填。

## A. Rome 官方 tariff importer 方法(Appendix: Rome Tariff Construction)

**来源**:`dataset/osm/zones/README_rome_tariff.md`、`script/build_rome_parking_tariff_geojson.py`、`tests/test_build_rome_parking_tariff_geojson.py`、输出 `dataset/osm/zones/rome_medium.geojson`。

要点(按附录叙述顺序):

1. 官方数据源是 Roma Mobilità "Sosta tariffata" ArcGIS 公开图层(blue-stripe parking polyline FeatureServer,字段 `TARIFFA`/`ZONA`/`AMBITO` 等);ZTL 中心边界图层保留作 provenance。
2. 官方小时费率 `0,50 / 1,00 / 1,20 EUR/h` 归一化为整数 tier `5 : 10 : 12`,**只保留官方相对比例**,绝对单位不解释为真实 cents/m——论文必须写明这一点,Rome E1 数字是 ratio-preserving,不是货币校准。
3. 街线到面元的转换:每个 100m tariff-grid cell 取最近 blue-stripe 街段的归一化费率;importer 对最大最近街段距离有显式上限,超限即失败,防止静默外推。
4. 边界事实:Rome E1 working block 只覆盖 tier `10` 和 `12`,未覆盖 tier `5`,因此 Rome economic signal 是保守下界(设计文档 §8.3)。

## B. E1 objective/billing drift 修复说明(Appendix: Adversary Objective Consistency)

**来源**:设计文档 §10.5、`common/settlement.py` 的 `fee_for_interval_values()`、`common/eval_harness.py`、回归测试 `tests/test_eval_harness.py`;修复前后对照目录 `experiments/e1_sweeps/`(pre-fix,勿引用)与 `experiments/e1_objective_fix/`、`experiments/e1_rome_real_tariff_ratio_fix/`(权威)。

要点:

1. 症状:修复前 Rome lifted-replay savings 报到 ~`0.94`。
2. 根因:adversary objective 用 `dist_m * rate` 直乘,而真实 billing 在 `dt > cadence_sec` 时走 fallback(`ceil(dt*vmax) * fallback_rate`);objective 与 billing 口径漂移,把 fallback 段的"省钱"严重高估。
3. 修复:`fee_for_interval_values()` 升级为 edge-level billing 的 single source of truth,E1/E3 的 RCSPP edge fee、E2 outage billing、`compute_bill` 全部走同一函数;新增 fallback-drift 回归测试。
4. 修复后 Rome lifted savings 降到 mean `0.0377` / p95 `0.117`(`experiments/e1_rome_real_tariff_ratio_fix/`)。
5. 方法论卖点:这就是论文 methodology 一节强调的 "objective/billing 同口径" 原则——任何经济消融实验若 objective 不等于真实计费函数,结果可以错一个数量级。

## C. E2 dwell-heavy native outage diagnostic(Appendix: Rome Native Outage Decomposition)

**来源**:`experiments/e2_fallback_frontier/tab_e2_outage_diagnostic.csv`、设计文档 §11.3。

要点:

1. Rome native gaps 的 all_outage 口径共 1088 个 edge,dwell edge share mean `0.633`,movement ratio median `0.0022`——即三分之二的 native outage 段几乎不动。
2. moving_outage 口径(过滤停驻后)剩 399 edges,dwell share `0.0`,movement ratio median `0.0099`。
3. 含义:Rome all-outage 的巨额 honest penalty(median 11.8×)主要来自"停着不动却按 `vmax` 距离收 fallback 费"的结构性放大,不是 fallback 机制对正常驾驶的惩罚;因此论文把 Rome native outage 报成 diagnostic upper bound,clean fairness anchor 用 GeoLife controlled outage。
4. `speed_bound_ok=True` 在所有行成立,E2 的激励兼容前提(`dist_i <= dt_i * vmax`)在数据上验证通过。

## D. Circuit soundness break 修复前后(Appendix: Comparator Bitwidth and max_dt)

**来源**:设计文档 §6.4、`circuits/settlement_period_v5_base.circom`;artifact 一致性指纹见 `README_ARTIFACT.md` §2。

要点:

1. 缺陷:早期电路 `dt` 只有宽松 range check,`dt^2` 与 `tier_vmax_sq * dt^2` 可超出 `LessEqThan(80)` 的安全位宽,在 field 上 wrap 后 continuity 约束可被绕过——这是二元 soundness break。
2. 修复:(a) 新增公共信号 `max_dt_sec=600` 并约束 `dt <= max_dt_sec`;(b) 收紧 `dtRange`;(c) `capTierRange` / `capModeRange` / `distSqRange` 显式 52-bit range check;(d) 比较器换 52-bit `stepTierLE` / `stepModeLE`。
3. 写作定位:此项写进 security analysis(soundness 修复),**不**作为 E1 经济消融表的一行——E1 行对应"约束存在但被关闭"的经济攻击面,而位宽缺陷属于"约束本身不成立"。
4. 修复后接口已封口:21 项 public signals 是协议接口,wrapper / prover / artifact `.sym` / vkey / charger 五方一致性由 `tests/test_prove_settlement_period_v5.py` 回归保护。

## E. (补充)争议仲裁选择性披露(Appendix: Dispute Opening,可选)

**来源**:`common/dispute.py`、`tests/test_dispute_opening.py`(2026-06-11 新增,设计文档 §21.4 #18)。

要点:仲裁方质疑单个 interval 时,prover 只开该 interval 的计费元组(level 1)或再加两端 raw fix(level 2),链上位置由 suffix 长度 + committed `fix_seq` 双重钉位;`opening_disclosure_profile()` 给出单次仲裁的精确披露字段清单,可并入 E5 泄漏账。一次 level-2 仲裁泄漏 = 1 个 interval 的 `(odo_delta, zone, rate, fee, fallback)` + 2 个 fix 的 `(time, cell, odometer)`,其余 23 个 interval 保持 opaque。
