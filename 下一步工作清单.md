# 下一步工作清单与改进计划

## 📊 当前实现完成度总览

### ✅ 已完成模块 (约45%完成度)
1. **基础联邦链路** - 80%完成
2. **客户端轨迹处理** - 70%完成  
3. **随机性与证明** - 60%完成
4. **Token化(CLOVER)** - 40%完成

### ⚠️ 部分完成/需改进
5. **差分隐私(Nebula)** - 30%完成
6. **多方计算(MPC)** - 20%完成
7. **TEE集成** - 0%完成

---

## 🔴 优先级P0: 核心功能缺失(立即开始)

### 1. Nebula差分隐私机制 【当前30% → 目标100%】

#### 1.1 阈值筛选(SVT)未实现
**文档位置**: 第三部分 3.2节 - 稀疏向量技术

**缺失内容**:
```python
# decoder/app.py 需要添加
def sparse_vector_technique(aggregated_counts, tau, epsilon_1):
    """
    稀疏向量技术:只输出≥tau的网格
    """
    # 1. 对阈值加噪声
    noisy_tau = tau + np.random.laplace(0, 2/epsilon_1)
    
    # 2. 对每个网格计数加噪声
    noisy_counts = {}
    for cell_id, count in aggregated_counts.items():
        noisy_count = count + np.random.laplace(0, 1/epsilon_1)
        if noisy_count >= noisy_tau:
            noisy_counts[cell_id] = noisy_count
    
    return noisy_counts
```

**当前问题**:
- ✗ `decoder/app.py`只有`/dump_latest_dp`端点,但未实际执行SVT
- ✗ 阈值`τ`未从环境变量读取
- ✗ 缺少ε₁和ε₂的隔离预算管理

**待实现**:
- [ ] 在`/reconstruct_latest`后添加SVT步骤
- [ ] 新增环境变量`THRESHOLD_TAU`(默认20)
- [ ] 实现`compute_epsilon_breakdown()`分解ε₁=0.3, ε₂=0.5
- [ ] 添加`/privacy_budget_status`端点显示当前消耗

#### 1.2 值噪声注入缺失
**文档位置**: 第三部分 3.3节

**缺失内容**:
```python
def add_value_noise(filtered_counts, epsilon_2, sensitivity):
    """
    对通过SVT筛选的网格值加拉普拉斯噪声
    """
    final_output = {}
    for cell_id, count in filtered_counts.items():
        # 敏感度 = max(dwell_time) * k
        noise = np.random.laplace(0, sensitivity / epsilon_2)
        final_output[cell_id] = max(0, count + noise)  # 非负截断
    
    return final_output
```

**待实现**:
- [ ] 在SVT之后添加值噪声阶段
- [ ] 从环境变量读取`SENSITIVITY`(默认50*300=15000秒)
- [ ] 实现后处理不变性检查(确保不泄露敏感度)

#### 1.3 Dummy注入未接入重构流程
**当前状态**: `client.py`已生成dummy,但服务器端未处理

**待实现**:
- [ ] 在`aggregator_a/app.py`的`/ingestA`中标记dummy来源
- [ ] 在`decoder/app.py`重构时识别并**移除**dummy对聚合结果的影响
- [ ] 添加统计:真实用户数 vs dummy数(用于验证p=0.3注入率)

**伪码**:
```python
# decoder端重构后
def remove_dummy_bias(final_map, dummy_count, real_user_count):
    """
    dummy会导致计数虚高,需按比例调整
    """
    scale_factor = real_user_count / (real_user_count + dummy_count)
    return {k: v * scale_factor for k, v in final_map.items()}
```

---

### 2. CLOVER Token化完善 【当前40% → 目标90%】

#### 2.1 双层PRP映射未端到端验证
**文档位置**: 第二部分 2.3节

**当前问题**:
- ✓ `utils.py`已有`prp_forward`/`prp_inverse`
- ✗ 客户端`client.py`调用PRP但**未验证逆映射正确性**
- ✗ 服务器端未实现解码时的PRP逆运算

**待实现**:
- [ ] 在`client.py`添加自检:
  ```python
  # 发送前验证
  original_idx = 42
  token = prp_forward(original_idx, key=..., domain=d)
  recovered = prp_inverse(token, key=..., domain=d)
  assert recovered == original_idx, "PRP映射损坏!"
  ```

- [ ] 在`decoder/app.py`的`/clover/decode_share`中:
  ```python
  def decrypt_indices(encrypted_indices, key_share_A, key_share_R):
      """
      两个聚合器协作解密索引
      """
      combined_key = xor_keys(key_share_A, key_share_R)
      original_indices = [
          prp_inverse(token, combined_key, DOMAIN) 
          for token in encrypted_indices
      ]
      return original_indices
  ```

#### 2.2 审计流程未完整
**文档位置**: 第四部分 4.2节

**当前状态**:
- ✓ `decoder/app.py`有`/clover/key_share`端点
- ✗ 缺少审计员`S_C`的签名验证
- ✗ 未实现配额限制(文档要求每轮最多3次解码)

**待实现**:
- [ ] 新增`AuditLog`模型存储解码请求:
  ```python
  audit_logs = {
      "round": 1,
      "auditor_signature": "...",
      "timestamp": 1704067200,
      "decoded_cells": [42, 123, 456],
      "quota_used": 1  # 本轮第几次解码
  }
  ```

- [ ] 添加配额检查:
  ```python
  @app.route('/clover/decode_share', methods=['POST'])
  def decode_share():
      round_id = request.json['round']
      if get_quota_used(round_id) >= 3:
          return {"error": "Quota exceeded"}, 429
      # ... 执行解码
      increment_quota(round_id)
  ```

- [ ] 实现签名验证(使用`cryptography`库):
  ```python
  from cryptography.hazmat.primitives.asymmetric import rsa, padding
  
  def verify_auditor_signature(payload, signature, public_key):
      public_key.verify(
          signature,
          payload,
          padding.PSS(...),
          hashes.SHA256()
      )
  ```

---

### 3. MPC安全聚合未实现 【当前20% → 目标80%】

#### 3.1 当前仅为明文聚合
**严重问题**: 
- 当前`aggregator_a/app.py`直接相加`share_a + share_r`,这**不是**真正的MPC!
- 任一聚合器都能看到重构后的明文向量

**文档要求**: 第四部分 4.3节 - 三方安全计算

**需替换为**:
```python
# 使用CrypTen或MP-SPDZ进行加密聚合
import crypten

def secure_aggregate(shares_A, shares_R):
    """
    加密域中聚合,单个服务器看不到中间结果
    """
    # 1. 将shares转为CrypTen张量
    enc_shares_A = crypten.cryptensor(shares_A, src=0)
    enc_shares_R = crypten.cryptensor(shares_R, src=1)
    
    # 2. 加密求和
    enc_sum = enc_shares_A + enc_shares_R
    
    # 3. 仅当Decoder请求时才解密
    return enc_sum  # 保持加密状态!
```

**待实现**:
- [ ] 安装CrypTen/PySyft框架
- [ ] 修改`aggregator_a/app.py`和`aggregator_r/app.py`使用加密聚合
- [ ] `decoder/app.py`通过MPC协议从两聚合器获取解密份额
- [ ] 添加单元测试验证"单个聚合器无法解密"

#### 3.2 未实现容错机制
**文档位置**: 第四部分 4.4节 - Robust Aggregation

**当前问题**: 如果某个客户端断线,整个聚合失败

**待实现**:
- [ ] Dropout容忍:只要≥N/2客户端上传即可聚合
- [ ] 超时处理:设置`BATCH_TIMEOUT=30s`,超时后强制flush
- [ ] 恶意份额检测:如果`share_a + share_r`重构出异常大值,拒绝该客户端

---

## 🟡 优先级P1: 功能完善(2周内完成)

### 4. 轨迹完整性验证改进 【当前60% → 目标95%】

#### 4.1 chi2阈值需动态校准
**当前问题**: `shuffler/app.py`使用固定阈值,误拒率可能过高

**文档建议**: 第二部分 2.4节 - 自适应阈值

**待实现**:
```python
# shuffler/app.py
def calibrate_threshold(historical_proofs, alpha=0.05):
    """
    基于历史数据调整阈值
    """
    # 1. 收集过去100轮的正常proof.s值
    normal_samples = [p['s'] for p in historical_proofs if p['accepted']]
    
    # 2. 拟合经验分布
    from scipy.stats import chi2
    empirical_threshold = np.percentile(normal_samples, 100*(1-alpha))
    
    # 3. 与理论阈值比较
    theoretical = chi2.ppf(1-alpha, df=10) * (B**2)
    
    return max(empirical_threshold, theoretical)  # 取较宽松者
```

- [ ] 添加`/calibrate_threshold`端点
- [ ] 每100轮自动重新校准
- [ ] 记录误拒率日志(正常用户被拒绝数/总请求数)

#### 4.2 恶意轨迹检测指标不足
**当前实现**: 仅检测"瞬移"(相邻点距离过大)

**文档扩展**: 应检测多种异常模式

**待添加**:
- [ ] **循环检测**: 轨迹在短时间内多次访问同一点(可能是GPS欺骗)
- [ ] **速度突变检测**: 速度从0突然到350km/h(非物理)
- [ ] **区域违规检测**: 访问了不可达区域(如海洋、军事禁区)

```python
# client.py
def advanced_trajectory_validation(trajectory):
    issues = []
    
    # 检测1: 循环访问
    visited = set()
    for point in trajectory:
        cell = (point.lat, point.lon)
        if cell in visited:
            issues.append(f"Revisited {cell}")
        visited.add(cell)
    
    # 检测2: 速度突变
    for i in range(len(trajectory)-1):
        v1 = compute_velocity(trajectory[i], trajectory[i+1])
        if i > 0:
            v0 = compute_velocity(trajectory[i-1], trajectory[i])
            accel = abs(v1 - v0) / delta_t
            if accel > 50:  # 50 m/s² 不合理
                issues.append(f"Sudden acceleration: {accel}")
    
    return len(issues) == 0, issues
```

---

### 5. 通信协议优化 【新增功能】

#### 5.1 批量上传未实现
**文档位置**: 第七部分 7.2节 - 效率优化

**当前问题**: 客户端每个时间窗口单独上传,高并发时服务器压力大

**待实现**:
- [ ] 客户端支持缓存多个窗口的数据,批量上传
- [ ] 服务器端支持`/batch_ingest`接收数组:
  ```python
  {
      "submissions": [
          {"round": 1, "shares_A": [...], ...},
          {"round": 2, "shares_A": [...], ...},
          ...
      ]
  }
  ```

#### 5.2 压缩未启用
**当前**: 使用JSON明文传输

**待实现**:
- [ ] 启用gzip压缩: `Content-Encoding: gzip`
- [ ] 对于大数组使用MessagePack替代JSON
- [ ] 预期通信量减少60%

---

## 🟢 优先级P2: 高级功能(1个月内)

### 6. TEE集成(可信执行环境) 【0% → 目标70%】

**文档位置**: 第二部分 2.6节 / 第四部分 4.1节

**核心目标**: 在TEE中生成秘密分享,防止恶意App窃取

#### 6.1 Android OP-TEE集成
**待实现**:
- [ ] 编写TA(Trusted Application)处理敏感操作:
  ```c
  // trustzone/secret_share_ta.c
  TEE_Result TA_InvokeCommandEntryPoint(...) {
      case CMD_GENERATE_SHARES:
          // 在TEE内生成shares,仅输出加密后的份额
          generate_additive_shares(plaintext, &share_A, &share_R);
          encrypt_share(share_A, server_A_pubkey);
          return TEE_SUCCESS;
  }
  ```

- [ ] Android客户端通过AIDL调用TA
- [ ] 验证普通App无法访问`plaintext`

#### 6.2 远程认证(Remote Attestation)
**待实现**:
- [ ] 服务器请求客户端提供TEE签名的证明:
  ```json
  {
      "attestation": {
          "platform": "OP-TEE",
          "nonce": "abc123",
          "signature": "...",  // TEE私钥签名
          "quote": "..."       // TEE状态哈希
      }
  }
  ```

- [ ] 服务器验证签名,确保数据确实来自TEE

---

### 7. 协作学习模块(输出B) 【0% → 目标60%】

**文档位置**: 第三部分 3.4节

**当前缺失**: 系统只输出热力图,未实现模型训练

#### 7.1 联邦学习集成
**待实现**:
```python
# decoder/app.py
@app.route('/train_model', methods=['POST'])
def train_federated_model():
    """
    基于私密热力图训练流量预测模型
    """
    # 1. 获取当前轮的DP热力图
    heatmap = get_latest_dp_output()
    
    # 2. 转为特征向量
    features = heatmap_to_features(heatmap)  # (time, cell_id) -> flow
    
    # 3. 联邦学习训练
    model = FlowPredictor()
    model.fit(features, labels, epochs=10)
    
    # 4. 模型本身也需满足DP
    dp_model = add_gradient_noise(model, epsilon=0.5)
    
    return {"model_accuracy": ..., "privacy_budget_used": 0.5}
```

**需新增**:
- [ ] 流量预测数据集生成脚本
- [ ] DP-SGD训练循环
- [ ] 模型隐私预算管理(与热力图独立)

---

## 🔧 代码重构建议

### 8. 配置管理改进

**当前问题**: 参数散落在多个文件的硬编码中

**建议**:
```yaml
# config/production.yaml
privacy:
  epsilon_total: 1.0
  epsilon_svt: 0.3
  epsilon_value: 0.5
  delta: 1.0e-8
  threshold_tau: 20

sparse_coding:
  top_k: 50
  quantization_bits: 4
  dummy_injection_rate: 0.3

integrity:
  risefl_samples_m: 10
  chi2_alpha: 0.05
  velocity_max_kmh: 350

system:
  batch_size: 100
  batch_timeout_sec: 30
  domain_size: 1000000
```

**待实现**:
- [ ] 使用`pydantic`加载配置
- [ ] 运行时热更新关键参数
- [ ] 配置文件版本控制

---

### 9. 日志与监控

**当前缺失**: 生产级日志

**待实现**:
```python
# utils/logging_config.py
import structlog

logger = structlog.get_logger()

# 在关键路径添加结构化日志
logger.info(
    "aggregation_completed",
    round_id=1,
    total_clients=10000,
    valid_submissions=9876,
    rejected_by_integrity=124,
    processing_time_ms=234,
    memory_usage_mb=456
)
```

**监控指标**:
- [ ] Prometheus metrics导出
- [ ] Grafana仪表板(吞吐量、延迟、拒绝率)
- [ ] 异常告警(如拒绝率突然>10%)

---

## 📋 测试补充

### 10. 单元测试覆盖率提升

**当前状态**: 缺少系统性测试

**待添加**:
```python
# tests/test_privacy_guarantees.py
def test_differential_privacy_epsilon():
    """验证实际ε不超过声明值"""
    config = {"epsilon": 1.0}
    
    # 生成邻近数据集
    D1 = generate_dataset(n_users=1000)
    D2 = remove_one_user(D1)
    
    # 运行机制100次
    outputs_D1 = [run_mechanism(D1, config) for _ in range(100)]
    outputs_D2 = [run_mechanism(D2, config) for _ in range(100)]
    
    # 计算KL散度上界
    epsilon_empirical = compute_max_divergence(outputs_D1, outputs_D2)
    
    assert epsilon_empirical <= 1.1, f"DP违约: {epsilon_empirical}"

# tests/test_integrity.py
def test_reject_malicious_trajectory():
    """测试拒绝瞬移轨迹"""
    # 构造恶意轨迹:1秒内从东京到大阪(500km)
    malicious = [
        (35.6895, 139.6917, 0),     # 东京
        (34.6937, 135.5023, 1)      # 大阪
    ]
    
    proof = generate_integrity_proof(malicious)
    result = verify_proof(proof)
    
    assert not result['accepted'], "未能拒绝恶意轨迹!"
    assert result['reason'] == 'chi2_over_threshold'
```

**覆盖目标**:
- [ ] 隐私保证测试 >10个场景
- [ ] 完整性验证测试 >20个异常模式
- [ ] 性能回归测试(防止新代码拖慢系统)

---

## 🎯 里程碑规划

### 第一阶段(2周): P0完成
- ✅ Nebula完整实现
- ✅ CLOVER解码验证
- ✅ MPC基础集成

**验收标准**: 
- 能在GeoLife数据集上端到端运行
- 输出热力图通过ε=1.0的DP验证
- 单个聚合器无法解密中间结果

### 第二阶段(2周): P1完成
- ✅ 轨迹验证自适应阈值
- ✅ 通信协议优化
- ✅ 配置管理重构

**验收标准**:
- 误拒率<5%
- 通信量降低50%
- 所有参数可通过配置文件修改

### 第三阶段(1个月): P2完成
- ✅ TEE集成(至少Android)
- ✅ 协作学习模块
- ✅ 完整测试套件

**验收标准**:
- TEE签名通过远程认证
- 训练的流量预测模型MAE<20%
- 测试覆盖率>80%

---

## 📝 文档更新清单

除了代码实现,你还需要更新以下文档:

- [ ] **API文档**: 新增端点的请求/响应格式
- [ ] **部署指南**: Docker Compose配置、环境变量说明
- [ ] **协议规范**: CLOVER/RiseFL/Nebula的详细流程图
- [ ] **实验复现说明**: 如何运行与论文相同的实验
- [ ] **故障排查指南**: 常见错误及解决方案

---

## 🚨 风险提示

### 高风险项
1. **MPC框架选择**: CrypTen vs MP-SPDZ性能差异大,需尽早测试
2. **TEE兼容性**: 部分Android设备不支持OP-TEE,需fallback方案
3. **DP参数校准**: ε=1.0可能对某些应用不够,需与用户需求平衡

### 技术债务
- 当前`share_a + share_r`的明文聚合**必须**替换为MPC
- Dummy注入逻辑未闭环,会导致错误的隐私分析
- 缺少端到端加密(客户端→服务器),存在中间人风险

---

## 总结

**当前完成度**: 约45%  
**剩余核心工作量**: 约3-4周全职开发  
**最紧急任务**: Nebula DP机制 + MPC替换明文聚合  

**建议优先级顺序**:
1. 先修复P0的Nebula和MPC(这是论文核心贡献点)
2. 再完善P1的工程质量
3. 最后添加P2的高级功能(TEE可作为可选扩展)

如果需要在2周内出原型Demo,可以:
- 暂缓TEE集成(用软件模拟)
- 简化协作学习(只输出热力图)
- 专注于**差分隐私保证的正确性验证**

需要我为某个具体模块(如Nebula或MPC)生成详细的实现代码吗?
